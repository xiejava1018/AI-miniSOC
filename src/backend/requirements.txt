# AI-miniSOC Backend Requirements

# FastAPI 核心依赖
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0

# 数据库
sqlalchemy==2.0.36
psycopg2-binary>=2.9.9,<3.0.0
asyncpg==0.30.0

# 认证和安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12

# HTTP 客户端
httpx==0.27.2
requests==2.32.3

# 数据库迁移 (alembic 与 sqlalchemy==2.0.36 兼容，>=1.10 支持 recursive_version_locations)
alembic>=1.13,<2.0

# 重试与容错
tenacity>=8.2,<10.0

# MCP (Model Context Protocol) — 让 Agent 通过 MCP 调用 FastAPI
# 注意：mcp<1.13 有 issubclass(param.annotation, Context) 启动崩溃；mcp>=1.20 引入 pyjwt>=2.10.1
# 与 zhipuai 的 pyjwt<2.9.0 冲突。限定为 1.13 ≤ mcp < 1.20，避免冲突
mcp>=1.13,<1.20
sse-starlette==3.4.8

# AI SDK (智谱AI)
zhipuai>=2.1.5

# 环境变量
python-dotenv==1.0.1

# 日志
loguru==0.7.2
python-json-logger>=2.0.0

# 可观测性
prometheus-client==0.21.0

# 工具
python-dateutil==2.9.0
pillow>=10.0.0
email-validator>=2.0.0
