# {项目名称}

{一句话描述}

## 技术栈

- 后端: {语言} {版本} / {框架} {版本}
- 前端: {框架} {版本} / {语言}
- 数据库: {数据库} {版本}
- 缓存: {缓存}
- 消息队列: {消息队列}
- 基础设施: Docker / {部署平台}
- CI/CD: {CI/CD平台}

## 架构概览

{简述核心架构模式，2-3句话}

```
核心数据流:
用户请求 → {网关/API层} → {业务层} → {数据层}
```

## 项目结构

```
{目录结构，每个目录一行注释说明用途}
```

## 开发规范

### 编码规范

**通用规则：**
- 遵循 {语言} 官方代码风格
- 单个函数不超过 50 行，单个文件不超过 300 行
- 公共 API 必须有类型注解/接口定义
- 错误处理：不要吞掉异常，必须向上传播或记录日志
- 日志：使用结构化日志，关键操作必须记录
- 魔法数字提取为常量

**命名规范：**
- 变量/函数: {snake_case / camelCase}
- 类/接口: {PascalCase}
- 常量: {UPPER_SNAKE_CASE}
- 文件名: {kebab-case / snake_case / PascalCase}
- 禁止拼音命名，禁止无意义缩写

**安全规范：**
- 所有外部输入必须校验和消毒
- SQL 查询使用参数化，禁止拼接
- 敏感信息不入日志
- 外部调用必须设置超时（HTTP 5s, DB 3s）

### Git 规范

**分支策略：**
```
main              # 稳定版本，受保护
develop           # 开发分支
feature/*         # 功能分支，从 develop 拉出
fix/*             # 修复分支
release/*         # 发版分支
```

**提交信息格式：**
```
<type>(<scope>): <subject>

<body>

<footer>
```

类型: feat | fix | docs | style | refactor | perf | test | chore | ci

**PR 规范：**
- 一个 PR 只做一件事
- 标题使用 conventional commits 格式
- 关联相关 Issue
- 通过所有 CI 检查才能合并

### 测试规范

- 新增功能必须有对应测试
- 测试覆盖率目标: > 70%
- 单元测试：测试业务逻辑
- 集成测试：测试 API 端点
- 测试文件与源文件同目录或 tests/ 下

## 关键约束

- 数据库迁移只加不改删
- API 版本化: `/api/v1/...`
- 所有配置通过环境变量注入
- 第三方依赖变更需评估安全性

## 基础设施

| 服务 | 地址 | 用途 |
|------|------|------|
| {代码仓库} | {地址} | 代码管理 |
| {数据库} | {地址:端口} | 业务数据 |
| {测试环境} | {地址} | 测试验证 |
| {监控} | {地址} | 系统监控 |
| {日志} | {地址} | 日志查询 |
| {CI/CD} | {地址} | 持续集成 |

## 常用命令

```bash
{安装命令}              # 安装依赖
{开发命令}              # 启动开发服务
{测试命令}              # 运行测试
{测试单个文件}          # 运行单个测试
{lint命令}             # 代码检查
{构建命令}              # 构建生产版本
{数据库迁移命令}        # 执行迁移
```

## 当前状态

### 进行中
- {当前迭代目标}

### 已知问题
- {已知 bug 或待解决事项}

## 注意事项

- 凭证在 .env 中管理，不硬编码，.env 已加入 .gitignore
- {根据技术栈补充特定注意事项}
- {已知陷阱}