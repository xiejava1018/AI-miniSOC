# {项目名称}

{一句话描述}

## 技术栈

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 后端 | {语言+框架} | {版本} | 业务逻辑 |
| 前端 | {框架} | {版本} | 用户界面 |
| 数据库 | {主库} | {版本} | 持久化存储 |
| 缓存 | {缓存} | {版本} | 热数据缓存 |
| 消息 | {消息队列} | {版本} | 异步通信 |
| 搜索 | {搜索引擎} | {版本} | 全文检索 |
| 基础设施 | Docker / K8s | - | 容器编排 |
| CI/CD | {平台} | - | 持续集成 |
| 监控 | {监控栈} | - | 可观测性 |

## 架构概览

### 系统架构

{详细描述系统架构，3-5句话}

```
                    ┌─────────────┐
                    │   CDN/LB    │
                    └──────┬──────┘
                           │
                    ┌──────┴──────┐
                    │  API Gateway│
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
        ┌─────┴─────┐┌────┴────┐┌─────┴─────┐
        │ Service A ││Service B││ Service C │
        └─────┬─────┘└────┬────┘└─────┬─────┘
              │            │            │
        ┌─────┴────────────┴────────────┴─────┐
        │            Data Layer                │
        └──────────────────────────────────────┘
```

### 核心设计原则

- **{原则1}**: {说明}
- **{原则2}**: {说明}
- **{原则3}**: {说明}

### 模块划分

| 模块 | 目录 | 职责 | 负责人/团队 |
|------|------|------|------------|
| {模块名} | {路径} | {职责} | {团队} |

## 项目结构

```
{完整目录结构，每个目录和关键文件都有注释}
```

## 开发规范

### 编码规范

**通用规则：**
- 遵循 {语言} 官方代码风格，通过 linter 强制执行
- 单个函数不超过 40 行，单个文件不超过 250 行，单个类不超过 500 行
- 圈复杂度不超过 10
- 公共 API 必须有完整的类型注解和文档注释
- 私有方法可以省略文档，但命名必须自解释
- 禁止使用 `any` / `Object` 等泛类型（除非确实必要并注释原因）

**命名规范：**
| 类型 | 风格 | 示例 |
|------|------|------|
| 变量/函数 | {风格} | `getUserById` |
| 类/接口 | PascalCase | `UserService` |
| 常量 | UPPER_SNAKE | `MAX_RETRY_COUNT` |
| 文件 | {风格} | `user-service.ts` |
| 数据库表 | snake_case | `user_roles` |
| API 路径 | kebab-case | `/api/v1/user-roles` |
| 环境变量 | UPPER_SNAKE | `DB_HOST` |

**错误处理：**
- 使用自定义异常体系，不要直接抛语言内置异常
- 异常必须包含错误码、上下文信息、原始原因
- 业务错误和系统错误分开处理
- 所有关键操作必须有回滚机制

**日志规范：**
```
级别使用:
  ERROR - 影响功能的错误，需要立即处理
  WARN  - 潜在问题，不影响主流程
  INFO  - 关键业务操作（用户登录、订单创建等）
  DEBUG - 调试信息，生产环境关闭

格式:
  {结构化日志格式示例}
```

**安全规范：**
- 所有外部输入必须校验、消毒、限长
- SQL 使用参数化查询，ORM 也必须检查是否安全
- 敏感信息（密码、Token、身份证号）不入日志、不入 URL
- 文件操作必须校验路径，防止路径穿越
- 外部调用必须设置超时和重试策略
- 密码使用 bcrypt/argon2 加密，禁止 MD5/SHA1

### API 设计规范

- RESTful 风格，版本化: `/api/v1/{resources}`
- 统一响应格式:
```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "timestamp": "2026-04-08T10:00:00Z",
  "requestId": "uuid"
}
```
- 分页参数: `page` + `pageSize`，默认 pageSize=20
- 错误码体系: {描述项目的错误码体系}
- 幂等性: 写操作必须支持幂等

### 数据库规范

- 表名 snake_case，复数形式
- 字段名 snake_case
- 每张表必须有 `id`, `created_at`, `updated_at`
- 金额使用精确类型（decimal），禁止浮点数
- 严禁 `SELECT *`，明确指定字段
- 迁移必须向后兼容，废弃字段标 deprecated 至少保留一个版本周期
- 索引命名: `idx_{表名}_{字段名}`

### Git 规范

**分支策略：**
```
main              # 生产分支，受保护，仅通过 PR 合并
develop           # 开发分支，日常集成
feature/*         # 功能分支，从 develop 拉出
fix/*             # 修复分支
release/v*        # 发版分支
hotfix/*          # 紧急修复，从 main 拉出
```

**提交信息格式：**
```
<type>(<scope>): <subject>

<body>

<footer>
```

| 类型 | 说明 |
|------|------|
| feat | 新功能 |
| fix | Bug 修复 |
| docs | 文档 |
| style | 格式（不影响功能） |
| refactor | 重构 |
| perf | 性能优化 |
| test | 测试 |
| chore | 构建/工具 |
| ci | CI/CD |
| revert | 回退 |

**scope** 必须指定影响的模块名。

**PR 规范：**
- 标题格式: `<type>(<scope>): <description>`
- 关联 Issue: `Closes #123` 或 `Refs #123`
- 通过所有 CI 检查
- 至少 1 人 Code Review 通过
- 不超过 400 行变更（超出需拆分）

### Code Review 规范

**必查项：**
- [ ] 功能是否正确实现
- [ ] 是否有安全漏洞
- [ ] 错误处理是否完善
- [ ] 是否有性能问题（N+1查询、内存泄漏等）
- [ ] 测试是否充分
- [ ] 是否符合编码规范

### 测试规范

- 新增功能 PR 必须包含测试
- 覆盖率目标: 核心逻辑 > 90%，整体 > 80%
- 测试分层:
  - **单元测试**: 业务逻辑、工具函数
  - **集成测试**: API 端点、数据库操作
  - **E2E 测试**: 关键业务流程
- 测试命名: `should_{expected_behavior}_when_{condition}`
- Mock 只用于外部依赖，内部模块用真实实现

## 关键约束

- 数据库迁移只加不改删
- API 必须版本化，v1 不可破坏性变更
- 所有配置通过环境变量注入
- 第三方依赖变更必须通过安全审查
- 超过 100 万行的表，查询必须带时间范围或索引条件
- 外部 API 调用必须有熔断和降级机制
- 生产环境禁止 DEBUG 级别日志

## 基础设施

| 服务 | 地址 | 用途 | 负责人 |
|------|------|------|--------|
| {代码仓库} | {地址} | 代码管理 | |
| {主数据库} | {地址:端口} | 业务数据（读写） | |
| {从数据库} | {地址:端口} | 只读查询 | |
| {缓存} | {地址:端口} | 热数据缓存 | |
| {测试环境} | {地址} | 测试验证 | |
| {预发布环境} | {地址} | 发版前验证 | |
| {生产环境} | {地址} | 生产服务 | |
| {监控} | {地址} | 系统监控 | |
| {日志} | {地址} | 日志查询 | |
| {APM} | {地址} | 性能监控 | |
| {CI/CD} | {地址} | 持续集成 | |
| {制品库} | {地址} | 镜像/包管理 | |

## 常用命令

```bash
# 开发
{安装命令}                          # 安装依赖
{开发命令}                          # 启动开发服务

# 测试
{测试命令}                          # 运行全部测试
{测试单个文件}                      # 运行单个测试文件
{测试覆盖率}                        # 生成覆盖率报告

# 代码质量
{lint命令}                         # 代码检查
{格式化命令}                        # 代码格式化
{类型检查}                          # 类型检查（如有）

# 数据库
{迁移命令}                          # 执行迁移
{回滚命令}                          # 回滚迁移
{种子数据}                          # 填充测试数据

# 构建 & 部署
{构建命令}                          # 构建生产版本
{Docker构建}                        # 构建镜像
{部署命令}                          # 部署到指定环境
```

## 当前状态

### 当前迭代: {迭代名称}
- 目标: {迭代目标}
- 截止: {日期}

### 进行中
- {当前任务}

### 已知问题
- {已知 bug、技术债务}

### 技术债务
- {待重构项}

## 注意事项

- 凭证在 .env 中管理，不硬编码，.env 已加入 .gitignore
- {根据技术栈补充特定注意事项}
- {已知陷阱}
- {性能注意事项}