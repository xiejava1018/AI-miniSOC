# P0 / P1 / P2 路线图完成度核查报告

> 核查日期：2026-08-16
> 核查方式：代码静态核对 + 真实环境（远程 Postgres `111.228.57.2:25432` / OpenSearch / 运行后端）端到端冒烟
> 结论：**P0 ✅ 完成；P1 ✅ 完成；P2 ⚠️ 功能完整但前端导航（菜单）未挂载，为唯一真实缺口**

---

## 一、P0 告警降噪聚合 + 告警-资产自动关联 + AI 研判落库 —— ✅ 完成

| 子项 | 证据 | 状态 |
|---|---|---|
| 告警降噪聚合（实时） | `alert_query.get_alert_groups()` 指纹=`rule_id\|agent_id`；`GET /api/v1/alerts/groups` 实测 `total_groups=31`，含 count/level/首次末次 | ✅ |
| 告警降噪聚合（历史快照） | `AlertGroupSnapshotService` + 调度器；`soc_alert_groups` 表 **1462 行** | ✅ |
| 告警-资产自动关联 | `alert_query._find_asset(agent_id, agent_ip)` 按 `Asset.wazuh_agent_id` / `Asset.asset_ip` 关联，返回 `linked_asset.asset_id`（查询期关联，因告警不落库、实时来自 Wazuh/Loki） | ✅ |
| AI 研判落库（按需） | `AIAnalysisService.analyze_alert` → `_save_analysis` 写 `soc_ai_analyses`；`POST /api/v1/ai/analyze` 端点 + MCP `ai_analyze_alert` | ✅（表 0 行仅因本环境未触发） |
| AI 研判自动化（群体） | `soc_alert_group_analyses` 表 **37 行**（告警治理 AI 研判 Phase 1 自动产出） | ✅ |

> 注：告警本身不存 `soc_alerts` 表，而是实时从 Wazuh/Loki 查询，故"告警-资产关联"为查询期按 `agent.id → asset.wazuh_agent_id` 关联，与资产详情页 M5 一致，逻辑贯通。

---

## 二、P1 事件闭环 + 脆弱性管理落地 —— ✅ 完成

### 1. 事件闭环
| 子项 | 证据 | 状态 |
|---|---|---|
| 事件后端 | `api/incidents.py`：GET 列表 / GET 详情 / POST 创建 / PUT 更新 / POST 时间线，全部存在 | ✅ |
| 事件数据 | `soc_incidents` 表 **13 条**，状态含 open | ✅ |
| 事件前端 | `views/incident/index.vue`：列表 + 状态流转（open→in_progress→resolved→closed）+ 处理说明 + 抽屉详情 + 时间线 | ✅ |
| 告警→事件关联 | `alert_incident_service.py` + `asset_incidents`（`soc_asset_incidents`）+ `POST /api/v1/incidents/{id}/from-alert`（脆弱性 M4 已建） | ✅ |
| 菜单挂载 | `soc_menus` 有「事件管理」(id=3) + 「事件列表」(id=21)，侧边栏可见 | ✅ |

### 2. 脆弱性管理
- 此前已独立验收通过（T0a+M0-M5 全量落地，`be6e2c9` 等提交，工作树干净）。本次不再重复，确认仍为完成态。

---

## 三、P2 上网行为异常检测 —— ⚠️ 功能完整，前端导航缺口

### 已完整实现（后端 + 前端页面 + 数据）
| 子项 | 证据 | 状态 |
|---|---|---|
| 检测规则引擎 | `services/browsing_detection/rule_engine.py`：**R1 黑名单域名 / R2 突发高频 / R3 新域名基线 / R4 隧道关键词 / R5 夜间活跃 / R6**，带权重 + `score_threshold` | ✅ 真规则，非空壳 |
| 采集/解析/调度/基线/情报 | `log_parser.py` / `scheduler.py` / `baseline_service.py` / `threat_intel.py` / `config.py` 齐全 | ✅ |
| 数据落库 | `soc_browsing_events` **1040 行**、`soc_browsing_baseline`、`soc_browsing_blacklist` 表均存在 | ✅ 真在产出 |
| 后端 API | `api/browsing.py`（/events、/logs、/config、/blacklist、/statistics、analyze） | ✅ |
| 前端页面 | `views/browsing/{event,blacklist,config,logs,statistics}/index.vue` 5 个页面 + `routesAlias` 别名齐全 | ✅ |
| 运行时 | `GET /api/v1/browsing/events` 实测 `total=1040`，返回真实域名（stun.chat.bilibili.com 等） | ✅ |

### ⚠️ 唯一真实缺口：前端导航（菜单）未挂载
- `soc_menus` 中**没有「上网行为 / browsing」任何菜单记录**（菜单树顶层仅有「事件管理」「告警管理」）。
- 本项目为**后端驱动菜单**模式，侧边栏由 `GET /menus/tree` 生成 → **browsing 5 个页面在 UI 导航中不可见**，只能凭直链 `/browsing/event/index` 访问。
- 存在初始化脚本 `scripts/init_browsing_menu.py`（应为补菜单用），但本环境未执行 → 菜单记录缺失。
- **影响**：功能 100% 可用，但用户从界面进不去。属"差最后一公里"级别缺口，修复成本极低（执行菜单种子脚本 / 插 `soc_menus` 记录）。

### 小项待确认（非阻断）
- `browsing_events` 的 `risk_score` 在接口返回中为 `null`（样本域名均无分值）。可能：① 早期写入未算分；② 字段名/序列化差异。建议确认评分回填链路。

---

## 四、总体判定

| 路线 | 判定 | 说明 |
|---|---|---|
| **P0** | ✅ 完成 | 降噪聚合 + 资产关联 + AI 研判（含自动化群体研判）全部落地且有真实数据 |
| **P1** | ✅ 完成 | 事件闭环（后端+前端+数据+菜单）与脆弱性管理均完成 |
| **P2** | ⚠️ 完成 95% | 检测引擎/数据/API/前端页面全在；仅缺 `soc_menus` 菜单挂载，导致导航不可见 |

**用户原判断「P0/P1/P2 应该都已完成」基本正确**；唯一需要补的是 P2 的菜单挂载（一个脚本/几条 INSERT 即可闭合）。

---

## 五、建议的下一步（可选）
1. **[P2 收尾]** 执行 `scripts/init_browsing_menu.py`（或手工 INSERT `soc_menus`），使上网行为 5 页出现在侧边栏 —— 建议立即做，成本极低。
2. **[P2 小修]** 确认 `browsing_events.risk_score` 为何为 null，修复评分回填。
3. 路线记忆更新：P1 由「部分完成」更正为「完成」，P2 由「未启动」更正为「功能完成、待菜单挂载」。
