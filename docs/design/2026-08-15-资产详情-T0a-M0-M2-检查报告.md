# 资产详情 T0a+M0-M2 实现检查报告（设计侧最终对账）

> 检查日期：2026-08-15 23:xx
> 检查对象：commit `22a4828` + 工作树未提交改动（pi agent 实施）
> 方法：代码静态核对 + 真实环境（远程 DB 可达）接口冒烟 + 前端 vue-tsc 类型检查
> 结论：**功能实现正确、可用；但提交状态不自洽、且 pi agent 超出约定范围实现了 M3，并有大量改动未提交。**

---

## 一、功能验证结果（实测，全部通过）

| 工单 | 验证方式 | 结果 |
|---|---|---|
| **T1** 摘要去伪 | 实测 `GET /assets/{id}/summary` | `vuln_critical=11 / vuln_high=61 / vuln_total=97 / sca_failed=0 / sca_pass_rate=None`，随资产变化，**非硬编码** ✓ |
| **T4** risk_score | 实测 `asset-vulnerabilities?asset_id&vuln_type=scap` | `CVE-2024-6789` cvss=5.0→risk_score=24.5（=5×10×0.4 + 资产关键度/暴露面系数），**计算正确** ✓ |
| **T4** vuln_type 过滤 | 同端点 `vuln_type=sca` | 正确分离 SCAP/SCA（sca 返回 0/117，视资产而定）✓ |
| **T4** fix_suggestion | SCA 项返回 | 字段有值 ✓ |
| **T2** SCA 卡判显 | 代码核对 vue L111–119 | `sca_pass_rate=null` 时显"SCA 不合规"+`sca_failed`+副标"合规率待接入(P5)" ✓ |
| **T3** 漏洞 Tab | 代码核对 vue L142–209 | 真实表格 + AI 风险列 + 生成事件按钮（`createIncidentFromVulnerability`）+ 无 Agent 空态 ✓ |
| **T5** 基线 Tab | 代码核对 vue L269–306 | `vuln_type=sca` 显不合规配置项 + 无 Agent 空态 ✓ |
| **M3**（超范围）应用清单 | 实测 `GET /assets/{id}/applications` | `code=200, total=704`，返回真实包（adduser/amd64-microcode/apparmor…）✓ |
| 前端类型 | `vue-tsc --noEmit` | **仅** `login/index.vue` 既有 `userInfo` 报错（与本次无关），其余 0 错误 ✓ |
| 后端启动 | uvicorn 启动 + `/health` | 200，无导入错误 ✓ |

> 实测说明：本环境 Bash 调用间后台进程会被回收，故每次冒烟均在**单次调用内**启动后端→测试→关闭；DB 为远程可达实例。首次冒烟出现的 `applications=836/704` 差异并非"旧后端应答"，而是 `applications` 已按资产真实统计（见下文范围偏离），数值随资产不同属正常。

---

## 二、重大缺陷：提交状态不自洽（必须处置）

1. **commit `22a4828` 引用了未提交模块**。
   - 已提交的 `asset_summary.py`（L24–33、L152–158）`from app.services.wazuh_inventory_service import WazuhInventoryService` 并调用其 `count_applications`。
   - 但 `src/backend/app/services/wazuh_inventory_service.py` **未跟踪（untracked）**，不在任何提交中。
   - → **`git checkout 22a4828` 干净检出后，后端启动/摘要接口会因 `ModuleNotFoundError` 崩溃**。本地能跑仅因为该文件恰在磁盘工作树中。
2. **大量改动未提交**：`git status` 当前显示
   - `?? wazuh_inventory_service.py`（M3 服务，被已提交代码依赖）
   - `?? merge_duplicate_assets_t0b.py`（T0b 重复资产合并脚本）
   - `M api/assets.py`（+72，`/applications` 端点）
   - `M asset_summary.py`（+22，M3 应用计数）
   - `M api/asset.ts`（+38，`getAssetApplications`）
   - `M views/asset/detail/index.vue`（+309，应用清单 Tab 接线等）
   - 即 pi agent 的"完成"含大量未提交工作，且中间提交 `22a4828` 不是自洽单元。

---

## 三、范围偏离（需你知悉）

- 约定批次为 **T0a + M0–M2**（不含 M3 / T0b）。pi agent 实际**额外实现了 M3（应用清单整模块）与 T0b（合并脚本）**。
- 这是净增益（M3 完成度高、端到端可用），但：
  - 偏离"最小闭环优先、M3 单独批次"的约定；
  - 导致提交拆分混乱（M3 代码散落未提交，且与 22a4828 交织）。

---

## 四、验证盲区（沙盒无法复现，须真实环境确认）

本沙盒连不到 192.168.x 私有网络，**以下断言无法独立核实，须真实环境复核**：

1. **T0a 物理唯一索引是否已在 DB 创建**：`asset.py` 仅加模型前向标注（`Index(..., postgresql_where=...)`），但项目用 `create_all` 启动、Alembic 非权威，**不会在已有表上自动建索引**。
   - 须真实环境执行：`SELECT indexname FROM pg_indexes WHERE tablename='soc_assets' AND indexname='uq_soc_assets_agent_id';`
   - 若不存在须补跑：`CREATE UNIQUE INDEX uq_soc_assets_agent_id ON soc_assets (wazuh_agent_id) WHERE wazuh_agent_id IS NOT NULL;`
2. **T0a 脏数据清理是否持久化**：commit message 称已按 Wazuh API 清 002/010 双挂，沙盒无法验证；真实环境复核：`SELECT wazuh_agent_id, count(*) FROM soc_assets WHERE wazuh_agent_id IS NOT NULL GROUP BY wazuh_agent_id HAVING count(*)>1;` 应返回 0 行。

---

## 五、处置建议（按优先级）

1. **【必须】补提交未提交改动**，使仓库自洽。建议拆 2 个提交：
   - 提交 A：M3 应用清单（`wazuh_inventory_service.py` + `assets.py` `/applications` + `asset_summary.py` 计数 + `asset.ts` + `vue` 应用清单 Tab）。
   - 提交 B：T0b 重复资产合并脚本（`merge_duplicate_assets_t0b.py`）。
   - 使 `22a4828` 之后的 HEAD 包含被其引用的 `wazuh_inventory_service.py`，消除"干净检出即崩溃"风险。
2. **【必须】真实环境核对 T0a 物理索引 + 脏数据清理**（见第四节）。
3. **【建议】** 为 T0a 索引补一个 Alembic 迁移（或部署文档注明"新环境需手动建索引"），避免后续环境漏建。
4. 其余功能（T1/T2/T3/T4/T5/M3）**无需返工**，质量达标。

---

## 六、一句话总结

> 代码写对了、能跑、端到端通；但**提交卫生差**——已提交的中间 commit 依赖一个未提交文件会致干净检出崩溃，且 M3/T0b 大量改动未提交。请先补提交 + 真实环境核对 T0a 索引，即可放心。
