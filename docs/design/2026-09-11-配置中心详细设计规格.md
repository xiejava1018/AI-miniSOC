# AI-miniSOC 配置中心 详细设计规格（落地版 v1.3）

> **文档定位**：本文是「平台配置能力梳理与配置化方案」的**工程落地规格**。目标读者是执行编码的 AI Agent / 开发工程师。
> 凡标注 **【事实】** 的均为代码核实结论；标注 **【规格】** 的为本次新增的设计要求，须严格实现。
> 需求背景、优先级论证、决策记录见：`docs/design/2026-09-11-平台配置能力梳理与配置化方案.md`

### 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-11 | 初稿 |
| v1.1 | 2026-09-11 | **代码核实后修订**：① 推翻 D5——迁移图实为单 head，改走 Alembic（§4.7、C6）；② 重写 §6.5 菜单 SQL（6 处错误：component 格式/path 相对名/icon 非 iconify/缺 permissions/硬编码 id/无幂等守卫）；③ 新增约定 C11–C12 |
| v1.2 | 2026-09-11 | **复核修订**：v1.1 的「单 head」前提经文件级复核**不成立**——误判为 **2 个 head**（`c1d2e3f4g5h6` + `f2a3b4c5d6e7`）。保留"走 Alembic"方向 |
| v1.3 | 2026-09-13 | **代码与实现全面对齐**：① 修正 C6——实为单 head `k6l7m8n9o0p1`（v1.2 误判，9238f78 已修复）；② §4.4 `soc_config_change_log` 已被 `f3g4h5i6j7k8` 删除，配置审计统一写 `soc_audit_logs`（CLAUDE.md v2.29）；③ §5.4.5 重写 `ConfigAuditService`——类名保留但内部转调 `AuditLogService.create_audit_log` 进 hash 链；④ §5.5 删 `/config-change-logs` 端点（合并到 `/audit-logs`）；⑤ §5.7 接入表补全——实际 19 个文件使用 resolver/ai_client；⑥ §6.1/6.5 调整——配置审计独立页已删（统一走审计日志页）、AI 模型管理页新增、菜单归属从 `/system` 改 `/config`（2026-09-13 顶级菜单重组）；⑦ 新增 §5.10 AI Provider 整套（CLAUDE.md v2.30）；⑧ §5.4.2 回落映射补 ai（GLM_*）；⑨ §5.6 路由 prefix 修正 `/data-sources`（复数） |

---

## 0. 如何使用本文档

### 0.1 执行顺序（必须按序，每步可独立验收）

| 步骤 | 内容 | 章节 | 完成判据 |
|---|---|---|---|
| S1 | 建表（3 张新表 + 1 处字段新增）**走 Alembic 迁移** | §4 | `soc_data_sources` 等 3 表存在，字段与 §4 一致；`alembic heads` 仍单 head |
| S2 | 后端模型 / Schema / Service | §5.2–5.4 | 可 import，单测通过 |
| S3 | 后端 API + 路由注册 | §5.5–5.6 | `/docs` 可见，CI 路由授权检查通过 |
| S4 | 解析器接入既有代码 | §5.7 | Wazuh/Loki/OpenSearch 读 DB，无 DB 行时回落 settings |
| S5 | 前端页面 + 菜单 | §6 | 页面可访问，CRUD 与测试连接可用 |
| S6 | 验收 | §8 | 全部 AC 通过 |

### 0.2 硬性工程约定（**违反会导致 CI 失败或运行期故障**）【事实】

| # | 约定 | 依据 |
|---|---|---|
| C1 | 后端必须从 `src/backend/` 目录启动，否则 `.env` 加载不到 | 项目约定 |
| C2 | 所有 HTTP 响应由中间件统一包装为 `{code, msg, data}`，**HTTP 恒 200**，业务码看 `body.code`；成功 `code=200`，创建 `code=201` | `app/core/response_wrapper.py` |
| C3 | 人类业务路由必须经 `include_human_router()` 注册；**写端点必须有显式授权依赖**（如 `require_admin()`） | `app/api/__init__.py` |
| C4 | CI 用例 `tests/security/test_route_authorization.py` 会扫描全部 `method+path`，要求命中 public / machine / human 三类之一。**新增路由后必须跑这个用例** | `app/api/__init__.py` 模块说明 |
| C5 | 新模型必须在 `app/models/__init__.py` 中导入，否则 `Base.metadata.create_all` 不会建表 | `app/models/__init__.py` |
| C6 | **Alembic 迁移图为单 head 线性**：`k6l7m8n9o0p1`（2026-09-13 文件级实测），共 56 个迁移文件、1 个 root `c5962ab1f662`，无分叉。CLAUDE.md 「迁移历史不完整」（`9238f78`）≠「多 head 分叉」是两个不同问题、不能互证。本期新表走迁移，`upgrade head` 一次性到顶 | 2026-09-13 文件级实测 |
| C7 | 表名必须避开已占用名，尤其 **`soc_asset_sources` 已存在**（资产多源溯源表） | 已核实 |
| C8 | 前端菜单**后端驱动**：仅加前端路由不显示，必须向 `soc_menus` 插记录并关联 admin 角色；**子菜单 `path` 用相对名、`component` 用带前导斜杠的实际路径、`permissions` JSONB 必填** | 项目约定 + §6.5 |
| C9 | 前端 `httpClient` 取 `res.data`；POST 带 Query 参数需拼 URL 查询串 | 项目约定 |
| C10 | 表统一 `soc_` 前缀 | 项目约定 |
| C11 | 菜单 `icon` 必须是 **iconify `ri:*`** 且在 `iconify-json/ri` 中真实存在；Material 风格字符串（`Database`/`Setting`/`Document`）**静默不渲染** | 迁移 `m1n2o3p4q5r6` 教训 |
| C12 | `soc_role_menus` **只有 `role_id` / `menu_id` / `permissions` 三列**，INSERT 不要带 `created_at` | 已核实（多次踩坑） |

---

## 1. 背景与目标

### 1.1 要解决的问题

| 问题 | 现状【事实】 | 影响 |
|---|---|---|
| P1 改配置要停服 | Wazuh/OpenSearch/Loki/AI 模型参数在 `.env`，改动需重启 uvicorn；MCP(8100) 同进程重启导致令牌全部失效 | 改配置 = 停监控，需重连 MCP 并重设凭据 |
| P2 配置无校验 | 系统配置页是通用 KV 编辑器，值类型手选，填错静默回落默认值 | 故障难排查 |
| P3 配错了不知道 | 全仓无连接测试能力 | 数据断流才发现 |
| P4 假配置误导 | 23 个幽灵配置已于 2026-09-11 清理（SMTP/钉钉/微信等） | 已解决 |
| P5 无变更审计 | `SystemConfig` 只有 `updated_by`，无版本历史 | 改坏无法追溯回滚 |

### 1.2 目标

| 目标 | 衡量方式 |
|---|---|
| G1 数据源对接参数可界面配置、热生效 | Wazuh/Loki/OpenSearch 改地址后 60s 内生效，无需重启 |
| G2 配置可校验、可测试 | 每个数据源支持"测试连接"，返回结构化结果 |
| G3 配置可追溯 | 所有变更入审计日志，敏感值脱敏 |
| G4 迁移期零中断 | DB 无配置时自动回落 `.env`，老部署不受影响 |

### 1.3 北极星指标

**配置变更平均生效耗时**：15 分钟 → **< 1 分钟**。
驱动指标：需重启才能完成的配置变更占比 90% → < 20%。

---

## 2. 范围

### 2.1 本期（v1.0）范围

| 编号 | 功能域 | 说明 |
|---|---|---|
| A | **数据源管理**（多实例） | Wazuh / OpenSearch / Loki 的增删改查、启停、设默认、测试连接 |
| B | **配置 Schema 注册表** | 声明式描述每个配置项的元信息，驱动表单渲染与校验 |
| C | **配置变更审计** | 记录谁在何时把什么从 A 改成 B |
| D | **解析器与回落** | 业务代码统一经解析器取配置，DB 优先、settings 兜底 |
| E | **AI 服务配置** | Provider 注册表（GLM / Claude CLI），模型与密钥可配 |

### 2.2 Non-goals（**明确不做，防止过度实现**）

| 不做什么 | 原因 |
|---|---|
| `SECRET_KEY` / `ENCRYPTION_KEY` / `DB_*` 界面化 | 改动导致全量 token 失效或必须重启，界面化无收益只造事故 |
| CORS 配置界面化 | 配错直接锁死前端 |
| 采集器远程配置下发 | 决策 D3 未定，留在 v2 |
| 通知渠道（邮件/钉钉/企微）实现 | 决策 D2 已定 P1 阶段再做，本期仅清理 |
| 数据保留策略执行 | 依赖清理任务，留 v2 |
| 配置文件热重载（如改 MCP 端口） | 属环境级，本期只标注"需重启" |
| 多 worker 下的缓存同步 | 当前单 worker 部署，多 worker 时改用 Redis 发布订阅 |

### 2.3 前置依赖

- PostgreSQL（已有）
- `ENCRYPTION_KEY` 已配置（已有，`app/services/encryption_service.py` 提供 `encrypt_if_needed` / `decrypt_if_needed`）
- `require_admin()` 权限依赖（已有，`app/core/permissions.py:29`）

---

## 3. 业务功能设计

### 3.1 角色与场景

| 角色 | 典型场景 | 权限 |
|---|---|---|
| **系统管理员 admin** | 首次部署时配置 Wazuh 对接；换 IP 后改地址并测试 | 全部读写 |
| **安全运营 operator** | 查看当前数据源及其健康状态 | 只读 |
| **viewer** | 只读查看 | 只读 |
| **AI Agent / MCP** | 经 MCP 工具读取数据源健康状态 | 只读（走既有 MCP 通道） |

> **权限矩阵【规格】**：`GET` 允许 admin/operator/viewer；`POST/PUT/PATCH/DELETE/测试连接` **仅 admin**。

### 3.2 用户故事

| ID | 用户故事 | 验收 |
|---|---|---|
| US-01 | 作为**系统管理员**，我希望在界面上配置 Wazuh 地址与账号，以便不用 SSH 改文件重启服务 | AC-01 |
| US-02 | 作为**系统管理员**，我希望配置完点"测试连接"立刻知道通不通、慢不慢，以便不用等数据不来才发现 | AC-02 |
| US-03 | 作为**系统管理员**，我希望平台能同时接两套 Wazuh（生产 + 分支），并指定哪套是默认 | AC-03 |
| US-04 | 作为**系统管理员**，我希望改配置时能看到它什么时候生效（立即/下周期/需重启），以便判断是否需要重启 | AC-04 |
| US-05 | 作为**系统管理员**，我希望密码类配置保存后界面不再显示明文，并且改错了能查到是谁改的 | AC-05、AC-06 |
| US-06 | 作为**安全运营**，我希望看到每个数据源当前是否健康、上次成功时间 | AC-07 |
| US-07 | 作为**系统管理员**，我希望删除被引用的数据源时被拦截而不是删完才发现出事 | AC-08 |

### 3.3 功能清单（FR）

| ID | 功能 | 优先级 | 说明 |
|---|---|---|---|
| FR-01 | 数据源列表 | P0 | 分页、按类型筛选、显示类型/地址/启用/默认/健康状态 |
| FR-02 | 新增数据源 | P0 | 表单 + 校验 + 测试连接 |
| FR-03 | 编辑数据源 | P0 | 密码留空表示不修改 |
| FR-04 | 删除数据源 | P0 | 被引用则拒绝（409） |
| FR-05 | 启用/停用 | P0 | 停用后不参与解析 |
| FR-06 | 设为默认 | P0 | 同类型至多一个默认 |
| FR-07 | **测试连接** | P0 | 按类型探测，返回结构化结果（§5.8） |
| FR-08 | 批量测试 | P1 | 列表页一键测试全部 |
| FR-09 | 配置 Schema 查询 | P0 | 供前端动态渲染表单 |
| FR-10 | 配置读写（复用既有 system-configs API） | P0 | 增加 schema 校验 |
| FR-11 | 配置变更审计查询【v1.3 合并】 | P0 | 不再有独立页面，统一到「审计日志」(`/audit-logs`)，按 `resource_type` 筛选 |
| FR-12 | 当前生效来源提示 | P0 | 界面显示"数据库 `wazuh-prod`"/"环境变量" |
| FR-13 | AI Provider 配置【v1.3 升级为独立模块】 | P0 | 详见 §5.10。从配置中心独立为独立业务对象（多实例 + 场景路由 + 60s 缓存） |

### 3.4 核心业务流程

#### 3.4.1 配置数据源

```
[数据源列表] → 新增 → [填写表单] → 测试连接 →
   ├─ 成功 → 保存（可选"设为默认"） → 写审计 → 60s 内生效
   └─ 失败 → 展示错误详情 → 允许"仍然保存"（二次确认，仅 admin）
```

> **产品要求【规格】**：测试连接失败时**允许仍然保存**（因为部署期数据源可能尚未就绪），但必须二次确认，且列表页该行显示红色"未通过"标记。

#### 3.4.2 配置解析（读取路径）

```
业务代码请求数据源(type=wazuh)
   → DataSourceResolver.resolve("wazuh")
        ├─ 缓存命中(60s) → 返回
        └─ 查 DB: type=wazuh AND enabled=true AND is_default=true
              ├─ 命中 → 解密 auth_secret → 构造配置 → 缓存 → 返回{config, origin:"db:xxx"}
              ├─ 未命中默认 → 查 type=wazuh AND enabled=true 任意一条 → 返回并 WARN"未设默认实例"
              └─ 无 → 回落 settings.WAZUH_API_URL 等 → 返回{config, origin:"env"}
```

#### 3.4.3 变更生效

| 变更类型 | 生效方式 | 界面标识 |
|---|---|---|
| 数据源连接参数 | 保存后 `invalidate()` 清缓存，下次读取即生效（≤60s） | 🟢 立即生效 |
| 采集范围 / 同步间隔 | 下一个调度周期 | 🟡 下个周期生效 |
| 服务端口 / MCP 配置 | 需重启 | 🔴 需重启服务 |

---

## 4. 数据模型

### 4.1 表清单

| 表名 | 状态 | 用途 |
|---|---|---|
| `soc_data_sources` | **新增** | 数据源多实例连接配置 |
| `soc_config_schema` | **新增** | 配置项元信息（驱动表单与校验） |
| `soc_config_change_log` | **【v1.3 删除】** | 配置变更审计**不再使用独立表**；统一写 `soc_audit_logs`（详见 §4.4）。迁移 `f3g4h5i6j7k8` 删除 |
| `soc_audit_logs` | **【v1.3 复用】** | 统一审计表（含配置变更，`resource_type=data_source/system_config/ai_provider`） |
| `soc_source_health` | **改动** | 新增可空字段 `source_id` |
| `soc_ai_providers` | **【v1.3 新增】** | AI 模型管理（独立于 `soc_data_sources`），详见 §5.10 |

### 4.2 `soc_data_sources`【规格】

> ⚠️ 勿与已存在的 `soc_asset_sources` 混淆（后者是"一条资产来自哪些数据源"的溯源表）。

| 字段 | 类型 | 空 | 默认 | 说明 |
|---|---|---|---|---|
| `id` | BigInteger PK autoincrement | 否 | | |
| `source_code` | String(64) **UNIQUE** | 否 | | 实例编码，如 `wazuh-prod`、`loki-main`。建议正则 `^[a-z][a-z0-9-]{2,63}$` |
| `source_type` | String(32) | 否 | | `wazuh` / `opensearch` / `loki` / `tplink` / `scanner` |
| `name` | String(200) | 否 | | 显示名 |
| `endpoint` | String(512) | 否 | | 完整地址，含协议与端口，**不以 `/` 结尾** |
| `auth_type` | String(32) | 否 | `none` | `basic` / `token` / `apikey` / `none` |
| `auth_username` | String(200) | 是 | | |
| `auth_secret` | Text | 是 | | **Fernet 加密存储**；API 出参永不返回 |
| `verify_ssl` | Boolean | 否 | `false` | Wazuh/OpenSearch 常为自签证书 |
| `timeout_seconds` | Integer | 否 | `30` | 1–300 |
| `retry_times` | Integer | 否 | `3` | 0–10 |
| `retry_backoff_seconds` | Integer | 否 | `2` | 1–60 |
| `enabled` | Boolean | 否 | `true` | |
| `is_default` | Boolean | 否 | `false` | 同 `source_type` 至多一个 `true` |
| `config_json` | JSONB | 是 | `'{}'` | 类型特有参数，见 §4.6 |
| `last_test_at` | DateTime(tz) | 是 | | |
| `last_test_ok` | Boolean | 是 | | |
| `last_test_message` | Text | 是 | | 截断至 1000 字符 |
| `created_at` | DateTime(tz) | 否 | `now()` | |
| `updated_at` | DateTime(tz) | 否 | `now()` | `onupdate=now()` |
| `updated_by` | BigInteger FK `soc_users.id` | 是 | | |

**约束**
- `UNIQUE (source_code)`
- 部分唯一索引：同一 `source_type` 下 `is_default=true` 至多一行
  ```sql
  CREATE UNIQUE INDEX uq_data_source_default_per_type
    ON soc_data_sources (source_type) WHERE is_default = true;
  ```
  若 DB 不支持部分索引，则在 Service 层用事务实现"设默认前先清零同类型其他行"。
- 索引：`(source_type, enabled)`

### 4.3 `soc_config_schema`【规格】

| 字段 | 类型 | 空 | 默认 | 说明 |
|---|---|---|---|---|
| `id` | BigInteger PK | 否 | | |
| `category` | String(50) | 否 | | 对应 `soc_system_config.category` |
| `key` | String(100) | 否 | | 对应 `soc_system_config.key` |
| `label` | String(200) | 否 | | 中文名 |
| `value_type` | String(20) | 否 | `string` | `string`/`number`/`boolean`/`json`/`password`/`multiline`/`list` |
| `default_value` | Text | 是 | | |
| `options` | JSONB | 是 | | 枚举：`[{"label":"低","value":"low"}, ...]` |
| `validation` | JSONB | 是 | | `{"min":1,"max":100,"maxLength":200,"pattern":"^..."}` |
| `effect_scope` | String(20) | 否 | `immediate` | `immediate`/`next_cycle`/`restart` |
| `sensitive` | Boolean | 否 | `false` | true 时出参脱敏、审计脱敏 |
| `group_name` | String(100) | 是 | | 页面内分组标题 |
| `sort_order` | Integer | 否 | `0` | |
| `help_text` | Text | 是 | | 输入框下方灰色提示 |
| `editable` | Boolean | 否 | `true` | false 时界面只读（如展示系统事实） |
| `created_at` / `updated_at` | DateTime(tz) | 否 | `now()` | |

**约束**：`UNIQUE (category, key)`

> **注意**：本表**不替代** `soc_system_config`，只描述其元信息。`soc_system_config` 缺行时由各业务模块的 `_DEFAULTS` 自注册（既有机制保留）。

### 4.4 配置变更审计【v1.3 重写】

> **【v1.3 重要变更】** 配置审计 **不再使用独立表 `soc_config_change_log`**。2026-09-12 发现该表数据
> 不进 hash 链、防篡改保护覆盖空白，迁移 `f3g4h5i6j7k8` 删除该表，**统一写入既有 `soc_audit_logs`**
> （resource_type=`data_source` / `system_config` / `ai_provider` 区分）。迁移同时删 `configLogs`
> 独立菜单与前端页面，用户在「审计日志」页面查全部。

**审计写入路径【事实】**：`ConfigAuditService.log(...)` 内部转调 `AuditLogService.create_audit_log(...)`，
自动参与 hash 链（详见 §5.4.5）。`soc_audit_logs` 字段（相关部分）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BigInteger PK | |
| `user_id` | BigInteger FK | 操作者 |
| `username` | String(50) | 操作者用户名快照（避免后续改用户表丢失） |
| `action` | String(50) | `create`/`update`/`delete`/`enable`/`disable`/`set_default`/`test` |
| `resource_type` | String(50) | `data_source` / `system_config` / `ai_provider` |
| `resource_id` | BigInteger | 目标 ID |
| `resource_name` | String(200) | 如 `wazuh-prod`、`alert_governance.triage_top_n` |
| `old_values` | JSONB | **敏感字段值替换为 `***`**（如 `auth_secret`、`api_key`） |
| `new_values` | JSONB | 同上 |
| `ip` | String(64) | 操作者 IP（X1 修复后从 token 补足，**不再固定为 `system`**） |
| `result` | String(20) | `success` / `failure`（测试连接用） |
| `log_hash` / `prev_log_hash` | String | hash 链防篡改【事实，CLAUDE.md v2.29】 |

### 4.5 `soc_source_health` 改动【事实 / 规格】

新增可空字段（**不加外键约束**，避免配置删除阻塞健康写入）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `source_id` | BigInteger | 指向 `soc_data_sources.id`，**无 FK 约束** |

> **【v1.3 事实】** `soc_source_health.source_id` 字段已于迁移 `e2f3g4h5i6j7` 加列（生产已跑），
> 但 **业务代码尚未使用该字段做反查**（仍按 `source_key` 字符串定位），字段保留供未来增量索引。
>
> `source_key` 语义收敛为 `{source_code}:{场景}`，例：`wazuh-prod:vulnerability_sync`。老 key 保持不动，不作数据迁移。

### 4.6 `config_json` 各类型约定【规格】

| source_type | config_json 键 | 示例 |
|---|---|---|
| `wazuh` | `collect_types` (array)、`agent_filter` (string) | `{"collect_types":["asset","vulnerability","baseline"],"agent_filter":""}` |
| `opensearch` | `index_patterns` (array) | `{"index_patterns":["wazuh-states-vulnerabilities-*","wazuh-alerts-*"]}` |
| `loki` | `tenant`、`default_labels` (object) | `{"tenant":"","default_labels":{"job":"tplink"}}` |
| `tplink` | `collect_types`、`interval_seconds` | `{"collect_types":["asset"],"interval_seconds":300}` |
| `scanner` | `max_rate`、`nmap_timeout` | `{"max_rate":100,"nmap_timeout":300}` |

> 未知键允许保留，不做强校验（向前兼容）。

### 4.7 建表路径【规格】

**采用 Alembic 迁移**（决策 D5）。

> **【v1.3 再修订：v1.2 的"2 个 head"结论也不成立】**
> v1.1 依据「实测 `alembic heads` = 单 head `c1d2e3f4g5h6`」推翻 v1.0；v1.2 复核后误判为 **2 个 head**
>（`c1d2e3f4g5h6` + `f2a3b4c5d6e7`）。**v1.3 再核实**：该"多 head"由 CLAUDE.md「迁移历史不完整」
>（`9238f78` 修复）**误读**而来——两讲不同问题、不能互证。CLAUDE.md v2.22 已明确写了
>「alembic check 仍有索引命名约定类 diff（pre-existing 噪音），单 head 线性」。
>
> **当前真相【事实，2026-09-13 文件级实测】**：
> ```
> heads:    ['k6l7m8n9o0p1']              # 唯一 head
> root:     ['c5962ab1f662']              # 唯一 root
> total:    56 个迁移文件                 # 不是 v1.2 写的 48 个
> ```
>
> **结论：直接 `upgrade head` 一步到顶，无需 merge。** create_all 的三个代价依然成立（`source_id`
> 加列 / 无 downgrade / 菜单种子），且项目确有 20+ 个"迁移里种菜单"的先例。
>
> 1. **`create_all` 不改已有表**——`soc_source_health.source_id` 这类加列必须手工 DDL，脱离版本管理，新环境重建必漏；
> 2. **无 downgrade**——上线出问题无法回退，与 `deploy.sh` 的自动回滚能力不匹配；
> 3. **菜单/权限种子无处安放**——§6.5 的 SQL 只能人工执行，与既有先例不一致。

**执行步骤**：

1. 新建 `app/models/data_source.py`、`app/models/config_schema.py`【事实：配置审计无独立 model，详情见 §4.4】
2. 在 `app/models/__init__.py` 中导入新模型（否则不进 `Base.metadata`，`alembic check` 会误报要 DROP）
3. 新建迁移，`down_revision` = **当前 head（执行前 `alembic heads` 现场确认）**：
   - `op.create_table()` 建表
   - `ALTER TABLE soc_source_health ADD COLUMN IF NOT EXISTS source_id BIGINT`（幂等写法，生产表已存在）
   - 种入 §6.5 的菜单与角色授权（**全 SQL 子查询写法**，见下）
   - `downgrade()` 对称删除表、删列、删菜单与授权
4. 本地空库验证：`DB_NAME=<临时库> ../../venv/bin/python -m alembic -c alembic.ini upgrade head`，
   再 `downgrade -1` → `upgrade head` 确认幂等
5. **生产由人工审阅后执行**（CI/CD 永不自动跑 alembic，见 CLAUDE.md）

> **迁移编写红线**（均为本项目已踩过的坑）：
> - 不要在迁移里用 Python 状态变量 + INSERT 后回读 SELECT——`--sql` dry-run 不执行 INSERT，回读必炸
> - 不要硬编码菜单/角色 id，一律 `INSERT ... SELECT ... WHERE NOT EXISTS` 子查询
> - `op.execute(text, params)` 双位置参数不兼容，改 `bind = op.get_bind(); bind.execute(...)`
> - JSONB 参数用 `CAST(:perms AS jsonb)`，**不要**写 `:perms::jsonb`（`::` 被当绑定参数）

---

## 5. 后端设计

### 5.1 文件清单【v1.3 与实现对齐】

**数据源 / 配置 schema 模块**

| 操作 | 路径 | 说明 |
|---|---|---|
| 新增 | `app/models/data_source.py` | `DataSource` 模型 |
| 新增 | `app/models/config_schema.py` | `ConfigSchema` 模型 |
| 修改 | `app/models/__init__.py` | 导入两个新模型 |
| 新增 | `app/schemas/data_source.py` | 入参/出参 Schema |
| 新增 | `app/schemas/config_schema.py` | Schema 元信息 |
| 新增 | `app/services/data_source_service.py` | CRUD + 设默认 + 审计 |
| 新增 | `app/services/data_source_resolver.py` | 解析 + 缓存 + 回落（含 `resolve_ai` 场景路由） |
| 新增 | `app/services/config_test_service.py` | 测试连接（Wazuh / OpenSearch / Loki / tplink / scanner / AI Provider） |
| 新增 | `app/services/config_audit_service.py` | 变更审计【v1.3 转调 `AuditLogService.create_audit_log`】 |
| 新增 | `app/services/config_validation.py` | Schema 驱动的值校验 + 删除保护（系统配置端点复用） |
| 新增 | `app/api/data_sources.py` | API 路由（详见 §5.5） |
| 修改 | `app/api/__init__.py` | 注册路由（**必须** `include_human_router`） |

**AI Provider 模块【v1.3 新增，详见 §5.10】**

| 操作 | 路径 | 说明 |
|---|---|---|
| 新增 | `app/models/ai_provider.py` | `AIProvider` 模型（独立于 `DataSource`） |
| 新增 | `app/schemas/ai_provider.py` | 入参/出参 Schema + `SCENES` 场景枚举 |
| 新增 | `app/services/ai_client.py` | 场景路由获取 provider + OpenAI 兼容调用封装 |
| 新增 | `app/services/ai_provider_service.py` | 增删改查 + 测试连接 + 设默认 + invalidate 缓存 |
| 新增 | `app/api/ai_providers.py` | `/api/v1/ai-providers` 路由 |
| 新增 | `scripts/seed_ai_provider.py` | 初始 seed |

**Scripts**

| 操作 | 路径 | 说明 |
|---|---|---|
| 新增 | `scripts/seed_config_schema.py` | 预置 schema 种子数据 |
| 新增 | `scripts/seed_datasources.py` | 预置 Wazuh / OpenSearch / Loki 3 个默认实例（生产部署后手跑） |
| 新增 | `scripts/seed_ai_provider.py` | 同上 |

> **禁止**修改 `app/core/config.py` 中已有字段语义；新增 `Settings` 字段仅限确实需要环境变量兜底的场景。
> **【v1.3 事实】** 2026-09-12 配置中心 v1 上线时 `GLM_API_KEY` 已从必填改为 `Optional[str] = None`，
> 因为正式配置走 `soc_data_sources` (source_type=`ai` 已退回) 或 `soc_ai_providers`，env 仅兑底。

### 5.2 模型代码骨架【规格】

`app/models/data_source.py`：

```python
"""数据源模型（多实例）"""
from sqlalchemy import Column, String, Text, Boolean, Integer, DateTime, BigInteger, ForeignKey, UniqueConstraint, Index
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.sql import func, text
from app.models.base import Base


class DataSource(Base):
    """数据源连接配置表（支持同类型多实例）"""
    __tablename__ = "soc_data_sources"

    id = Column(BigInteger, primary_key=True, autoincrement=True)
    source_code = Column(String(64), nullable=False, unique=True)
    source_type = Column(String(32), nullable=False)
    name = Column(String(200), nullable=False)
    endpoint = Column(String(512), nullable=False)
    auth_type = Column(String(32), nullable=False, default="none")
    auth_username = Column(String(200), nullable=True)
    auth_secret = Column(Text, nullable=True)          # Fernet 加密
    verify_ssl = Column(Boolean, nullable=False, default=False)
    timeout_seconds = Column(Integer, nullable=False, default=30)
    retry_times = Column(Integer, nullable=False, default=3)
    retry_backoff_seconds = Column(Integer, nullable=False, default=2)
    enabled = Column(Boolean, nullable=False, default=True)
    is_default = Column(Boolean, nullable=False, default=False)
    config_json = Column(JSONB, nullable=False, server_default=text("'{}'::jsonb"))
    last_test_at = Column(DateTime(timezone=True), nullable=True)
    last_test_ok = Column(Boolean, nullable=True)
    last_test_message = Column(Text, nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
    updated_by = Column(BigInteger, ForeignKey("soc_users.id"), nullable=True)

    __table_args__ = (
        Index("ix_data_source_type_enabled", "source_type", "enabled"),
    )
```

部分唯一索引（在 Service 初始化或 SQL 脚本中执行）：

```sql
CREATE UNIQUE INDEX IF NOT EXISTS uq_data_source_default_per_type
  ON soc_data_sources (source_type) WHERE is_default = true;
```

### 5.3 Schema【规格】

`app/schemas/data_source.py` 要点：

| 类 | 字段 | 说明 |
|---|---|---|
| `DataSourceCreate` | `source_code`(pattern `^[a-z][a-z0-9-]{2,63}$`)、`source_type`(Enum)、`name`、`endpoint`、`auth_type`、`auth_username?`、`auth_secret?`、`verify_ssl`、`timeout_seconds`(1–300)、`retry_times`(0–10)、`retry_backoff_seconds`(1–60)、`enabled`、`is_default`、`config_json?` | 新建 |
| `DataSourceUpdate` | 同上全部可选；**`auth_secret` 传 `null`/空串表示不修改** | 更新 |
| `DataSourceResponse` | 全部字段 **但不含 `auth_secret`**；新增派生字段 `has_secret`(bool)、`secret_masked`(str, 固定 `"******"`)、`health_status` | 出参 |
| `DataSourceListResponse` | `items`、`total`、`page`、`page_size` | 列表 |
| `TestConnectionRequest` | `draft`(DataSourceCreate) 或 `id`(int)，二选一 | 测试连接 |
| `TestConnectionResponse` | `ok`(bool)、`latency_ms`(int)、`details`(dict)、`checks`(list of `{name, ok, message}`)、`message`(str) | 测试结果 |

> **安全约束【规格】**：`DataSourceResponse` 中**绝不能**出现 `auth_secret` 明文或密文。Pydantic 模型不声明该字段即可。

### 5.4 Service 层【规格】

#### 5.4.1 `DataServiceService`（CRUD）

| 方法 | 行为 |
|---|---|
| `list(...)` | 分页 + `source_type` 过滤 + 关键字搜索（code/name） |
| `get_by_id` / `get_by_code` | |
| `create` | 校验 `source_code` 唯一；若 `is_default=True` → 事务内先 `UPDATE ... SET is_default=false WHERE source_type=?`；`auth_secret` 经 `encryption_service.encrypt()` 后存储；写审计 `create` |
| `update` | `auth_secret` 为空/None 时**保持原值不变**；改 `is_default` 同上；写审计 `update`，`changed_fields` 记录实际变更字段；**保存后调用 `resolver.invalidate(source_type)`** |
| `delete` | **删除保护**（见 5.4.4）；写审计 `delete`；invalidate |
| `set_default(id)` | 事务内清零同类型其他行 → 置本行 true → 审计 |
| `test(id 或 draft)` | 委托 `ConfigTestService`，结果写回 `last_test_*` |

#### 5.4.2 `DataSourceResolver`（核心）

```python
CACHE_TTL = 60  # 秒，与既有 alert_governance_config / browsing config 保持一致

def resolve(source_type: str) -> ResolvedConfig:
    """返回 ResolvedConfig(config: dict, origin: str, source_code: str|None)

    origin 取值: "db:{source_code}" | "env" | "none"
    """
```

- 缓存 key = `source_type`；缓存值 = `ResolvedConfig`
- `invalidate(source_type=None)`：不传则清空全部
- **回落顺序**（§3.4.2）：默认实例 → 任意启用实例（记 `logger.warning`）→ `settings.*`
- 回落映射：

| source_type | 回落字段 |
|---|---|
| `wazuh` | `settings.WAZUH_API_URL` / `WAZUH_API_USERNAME` / `WAZUH_API_PASSWORD` |
| `opensearch` | `settings.OPENSEARCH_URL` / `OPENSEARCH_USER` / `OPENSEARCH_PASSWORD` |
| `loki` | `settings.LOKI_API_URL` |
| `ai`【v1.3 新增】 | `settings.GLM_API_KEY` / `GLM_MODEL` / `GLM_API_BASE`（OpenAI 兼容协议兑底） |
| `tplink` / `scanner` | 无回落（必须配置） |

> **【v1.3 说明】** `ai` 类型回落与其它类型不同——**不读 `soc_data_sources`，读 `soc_ai_providers`**（独立表，
> 独立业务模型）。其入口为 `DataSourceResolver.resolve_ai(scene=...)`（§5.10）。

#### 5.4.3 `ConfigTestService` 测试连接规格（**见 §5.8**）

#### 5.4.4 删除保护【规格】

删除前检查引用，命中则抛 `409 Conflict` 并指明引用方：

| 检查项 | 依据 |
|---|---|
| 是否存在 `enabled=true` 且为**唯一**同类型实例，且该类型正被运行中调度使用 | 由调用方通过 `register_reference()` 注册；v1 简化为：若该实例是同类型唯一启用实例且有 `last_success_at` 记录 → 拒绝删除，提示"请先停用或更换默认实例" |

> v1 实现为**保守策略**：唯一启用实例不允许删除，必须先停用或新建替代实例。

#### 5.4.5 `ConfigAuditService`【v1.3 重写】

> **【v1.3 重要变更】** 配置审计统一写 `soc_audit_logs`（CLAUDE.md v2.29）。
> `ConfigAuditService` 类名保留（6 处调用点），但内部转调 `AuditLogService.create_audit_log()`，
> 自动参与 hash 链（`log_hash` / `prev_log_hash`）防篡改保护。

```python
def log(
    target_type: str,           # 'data_source' | 'system_config' | 'ai_provider'
    target_key: str,            # 'wazuh-prod' | 'alert_governance.triage_top_n' | 'glm-main'
    action: str,                # 'create'/'update'/'delete'/'enable'/'disable'/'set_default'/'test'
    before: Any = None,         # dict / model —— 会被 _mask_sensitive + _jsonify 处理
    after: Any = None,
    changed_fields: Iterable[str] | None = None,
    operator_id: int | None = None,    # 从 token 补足（X1 修复后不再固定为 'system'）
    operator_ip: str | None = None,
    result: str = 'success',    # 'success' | 'failure'
)
```

**内部行为【事实】**：
- `_mask_sensitive(payload)` 递归把 dict 中以下键（不区分大小写）的值替换为 `"***"`：
  `auth_secret` / `password` / `secret` / `token` / `api_key` / `apikey` / `private_key` / `access_key`
- `_jsonify(payload)` 递归把 `datetime` / `date` / `Decimal` 转 ISO 字符串（`soc_audit_logs.old/new_values` 是 JSONB，原生不支持）
- 内部调 `AuditLogService.create_audit_log(...)`，传 `resource_type=target_type`、`resource_name=target_key`、`old_values=before`（处理后）、`new_values=after`（处理后）
- 写入失败**不得阻断主流程**，仅 `logger.exception`

> **【v1.3 教训】** v1 写独立表 `soc_config_change_log`，绕过 hash 链——属安全覆盖空白。
> v2（`f3g4h5i6j7k8` 迁移）合并进 `soc_audit_logs`，同时删 `configLogs` 菜单与页面。

### 5.5 API 清单【规格，v1.3 调整】

#### 5.5.1 数据源管理

前缀：`/api/v1/data-sources`（**复数**，v1.0 文档写 `/data-source` 单数是错的—— `include_human_router` prefix 与 endpoint path 会双重叠加）

| # | Method | Path | 权限 | 说明 |
|---|---|---|---|---|
| 1 | GET | `/data-sources` | 登录用户 | 列表，query: `page`、`page_size`、`source_type`、`search` |
| 2 | GET | `/data-sources/types` | 登录用户 | 返回支持的类型枚举与默认字段模板 |
| 3 | GET | `/data-sources/resolve-status` | 登录用户 | 返回各类型当前生效来源（用于界面"当前生效来源"提示） |
| 4 | GET | `/data-sources/{id}` | 登录用户 | 详情（不含密钥） |
| 5 | POST | `/data-sources` | **admin** | 新建 |
| 6 | PUT | `/data-sources/{id}` | **admin** | 更新 |
| 7 | DELETE | `/data-sources/{id}` | **admin** | 删除（受 5.4.4 保护） |
| 8 | PATCH | `/data-sources/{id}/enabled` | **admin** | body: `{"enabled": bool}` |
| 9 | POST | `/data-sources/{id}/set-default` | **admin** | 设为同类型默认 |
| 10 | POST | `/data-sources/test` | **admin** | 测试连接，body 支持 `{id}` 或 `{draft:{...}}` |

#### 5.5.2 配置 Schema【v1.3 调整】

| # | Method | Path | 权限 | 说明 |
|---|---|---|---|---|
| 11 | GET | `/config-schemas` | 登录用户 | 按 `category` 返回 schema 列表 |

#### 5.5.3 系统配置（既有 API，配置中心复用）

| # | Method | Path | 权限 | 说明 |
|---|---|---|---|---|
| 12 | GET | `/system-configs/categories` | 登录用户 | 所有 category 列表 |
| 13 | GET | `/system-configs/by-category/{category}` | 登录用户 | 单 category 下所有 KV |
| 14 | GET/POST/PUT/DELETE | `/system-configs[/{id}]` | **admin** 写 | CRUD（CLAUDE.md「迁移到数据源管理」之外的非数据源类配置仍走此处） |

#### 5.5.4 配置审计【v1.3 变更】

| # | Method | Path | 权限 | 说明 |
|---|---|---|---|---|
| ~~旧~~ | ~~GET /config-change-logs~~ | — | — | **【v1.3 删除】** `soc_config_change_log` 表与独立配置审计页面已下线 |
| 15 | GET | `/audit-logs` | **admin/auditor**（CLAUDE.md X1） | 查全部审计（含配置变更），按 `resource_type` 筛选 |
| 16 | GET | `/audit-logs/{id}` | admin/auditor | 单条审计详情 |
| 17 | POST | `/audit-logs/export` | admin/auditor | CSV 导出 |

#### 5.5.5 AI Provider【v1.3 新增，详见 §5.10】

前缀：`/api/v1/ai-providers`

| # | Method | Path | 权限 | 说明 |
|---|---|---|---|---|
| 1 | GET | `/ai-providers` | 登录用户 | 列表（按 `search` 过滤 provider_code/name） |
| 2 | GET | `/ai-providers/scenes` | 登录用户 | 场景枚举（9 个场景，详见 §5.10.2） |
| 3 | GET | `/ai-providers/{id}` | 登录用户 | 详情 |
| 4 | POST | `/ai-providers` | **admin** | 新建 |
| 5 | PUT | `/ai-providers/{id}` | **admin** | 更新 |
| 6 | DELETE | `/ai-providers/{id}` | **admin** | 删除 |
| 7 | POST | `/ai-providers/{id}/set-default` | **admin** | 设为默认实例 |
| 8 | POST | `/ai-providers/test` | **admin** | 测试连接（body 支持 `{id}` 或 `{draft:{...}}`） |

**响应示例（测试连接）**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "ok": true,
    "latency_ms": 128,
    "message": "连接成功",
    "checks": [
      {"name": "认证", "ok": true, "message": "JWT 获取成功"},
      {"name": "版本", "ok": true, "message": "4.7.2"},
      {"name": "Agents 接口", "ok": true, "message": "返回 1 条"},
      {"name": "漏洞接口", "ok": false, "message": "404 — 本环境不支持，CVE 走 OpenSearch"}
    ],
    "details": {"api_version": "4.7.2"}
  }
}
```

### 5.6 路由注册【v1.3 调整】

> **【v1.3 提醒】** `include_human_router` 的 prefix 与 endpoint path 会双重叠加。
> 例：`prefix="/data-sources"` + `@router.post("")` 得到 `/api/v1/data-sources`（与前端 `BASE` 一致），
> 但若 prefix 写 `/data-source` 单数则会错位为 `/api/v1/data-source`。

在 `app/api/__init__.py` 中【事实，2026-09-13 实测】：

```python
from app.api import (
    ..., data_sources, config_schemas, ai_providers,
)

# 数据源管理（prefix 与 router endpoint 拼出 /api/v1/data-sources）
include_human_router(
    api_router, data_sources.router, prefix="/data-sources", tags=["数据源管理"],
)
# 配置 Schema（只读 GET）
include_human_router(
    api_router, config_schemas.router, prefix="/config-schemas", tags=["配置 Schema"],
)
# AI Provider（详见 §5.10）
include_human_router(
    api_router, ai_providers.router, prefix="/ai-providers", tags=["AI 模型管理"],
)
```

并在 `data_sources.py` / `ai_providers.py` 内为**写端点**显式声明：

```python
from app.core.permissions import require_admin

@router.post("", dependencies=[Depends(require_admin())])
async def create_data_source(...): ...
```

> **必须**：注册后运行 `pytest tests/security/test_route_authorization.py`，确保新路由被正确归类（约束 C3/C4）。

### 5.7 与既有代码的接入（回落兼容）【v1.3 补全】

> **【v1.3 事实】** 文档 v1.0 只列了 3 个接入点（wazuh_client / loki_client / opensearch_scap_sync），
> 但配置中心 v1 上线后实际接入了 19 个文件。其中**大部分走 `ai_client` 而不是 resolver**
> （因为 AI 调用有场景路由）。

**Resolver 接入（数据源型）**——`from app.services.data_source_resolver import ...`：

| 调用点 | 用途 |
|---|---|
| `app/api/data_sources.py` | `data_source_resolver.resolve_status(db)` 给前端返回 5 类当前生效来源 |
| `app/services/dashboard_service.py` | dashboard 摘要走 `resolve("opensearch")` / `resolve("loki")` |
| `app/services/wazuh_client.py` | `resolve("wazuh")` 获取 endpoint / 账号（CLAUDE.md v2.30 3c 阶段） |
| `app/services/opensearch_scap_sync.py` | `resolve("opensearch")` 读 CVE 索引 |
| `app/services/browsing_detection/loki_client.py` | `resolve("loki")` |
| `app/services/wazuh_inventory_service.py` | 资产入库走 `resolve("wazuh")` |
| `app/services/behavior_profile/loki_source.py` | 行为画像聚合走 `resolve("loki")` |
| `app/services/behavior_profile/identity.py` | 行为画像身份绑定 |
| `app/api/internal/tools.py` | MCP 内部工具走 `resolve("loki")` |
| `app/mcp/tools/loki_tools.py` | MCP 服务走 `get_endpoint("loki")` 便捷函数 |

**AI Client 接入（场景路由型）**——`from app.services.ai_client import get_provider_by_scene, chat, ...`：

| 调用点 | 场景 | 用途 |
|---|---|---|
| `app/services/ai_analysis.py` | `ai_analysis` | 告警研判 |
| `app/services/asset_query.py` | `asset_query` | L1/L2 自然语言资产查询 |
| `app/services/asset_risk.py` | `risk` | 风险评分补充解读 |
| `app/services/asset_security.py` | `risk` | 资产安全解读 |
| `app/services/behavior_profile/ai_summary.py` | `behavior` | 行为画像 AI 解读 |
| `app/services/compliance_ai.py` | `compliance` | 合规 AI 解读 |
| `app/services/impact_analysis.py` | `impact` | 变更影响分析 |
| `app/services/knowledge_service.py` | `knowledge` | 知识库生成 |
| `app/services/reconcile_ai.py` | `reconcile` | 资产稽核 AI 解读 |
| `app/services/report_generator.py` | `report` | 安全报告生成 |

**接入原则【v1.3 补充】**：
- **不修改**构造函数签名语义（显式传参仍最高优先级）
- resolver 返回 `origin=None`（DB 与 env 都没有）时，保持现有行为不变
- 所有 resolver 调用必须 `try/except`，失败回落 `settings`，**不得因配置读取失败导致业务中断**
- AI Client 场景路由顺序：精确场景匹配 → 默认实例 → 任意启用实例（WARN）→ env GLM_* 兑底

### 5.8 测试连接实现规格【规格】

统一约定：
- 超时取 `min(timeout_seconds, 15)`，硬上限 15s
- 结果写回 `last_test_at` / `last_test_ok` / `last_test_message`（截断 1000 字符）
- `verify_ssl=False` 时 `httpx.Client(verify=False)`（与现有 `wazuh_client.py:36` 一致）
- 任何异常都要捕获并转为 `ok=false` + 可读 message，**不得抛 500**

| source_type | 探测步骤 | 成功判据 |
|---|---|---|
| `wazuh` | ① `POST {endpoint}/security/user/authenticate`（basic auth）→ 取 `data.token`；② `GET {endpoint}/` 带 Bearer → 取 `data.api_version`；③ `GET {endpoint}/agents?limit=1` → 取 `affected_items`；④ `GET {endpoint}/vulnerability?limit=1` → **探测**，404 记 `ok=false` 但**不影响总判定** | ①②③ 全通过 |
| `opensearch` | ① `GET {endpoint}/` → `version.number`、`cluster_name`；② `GET {endpoint}/_cluster/health` → `status` | ① 通过；② 中 `status` 为 `red` 时 `ok=true` 但 message 告警 |
| `loki` | ① `GET {endpoint}/ready`；② `GET {endpoint}/loki/api/v1/labels` | ① 通过；② 失败仅告警 |
| `tplink` | 仅做 TCP 连通性探测（`socket.create_connection`，3s）。**不做真实登录**，避免触发路由器风控 | 端口可连 |
| `scanner` | `GET {endpoint}/health` | HTTP 200 |

> **已知环境事实**（`wazuh_client.py:8-10` 注释）：本环境 Wazuh API **无 `/vulnerability` 路由**（全 404），CVE 真实数据源是 OpenSearch `wazuh-states-vulnerabilities-*`。测试连接第 ④ 步须把这一事实明确告知用户，这是本功能的高价值点。

### 5.9 配置 Schema 种子数据【规格】

`scripts/seed_config_schema.py` 预置（至少覆盖现有一个完整域，作为范式）：

| category | key | label | type | default | effect_scope |
|---|---|---|---|---|---|
| `alert_governance` | `triage_top_n` | 每日 AI 研判 TopN 簇数 | number | 20 | immediate |
| `alert_governance` | `min_group_count` | 告警簇最小条数 | number | 1 | immediate |
| `alert_governance` | `suppress_rule_ids` | 噪声抑制规则 ID | string | "" | immediate |
| `browsing_detection` | `enabled` | 启用检测 | boolean | true | next_cycle |
| `browsing_detection` | `interval_seconds` | 检测间隔（秒） | number | 300 | next_cycle |
| `browsing_detection` | `score_threshold` | 告警分数阈值 | number | 50 | next_cycle |
| `browsing_detection` | `whitelist_domains` | 域名白名单 | multiline | ... | next_cycle |
| `browsing_detection` | `night_start_hour` | 夜间起始小时 | number | 2 | next_cycle |

> 脚本必须**幂等**：`UNIQUE(category,key)` 冲突时跳过或更新非敏感字段。

### 5.10 AI Provider 模块【v1.3 新增整章】

> 2026-09-13 AI 模型管理从「配置中心」独立为一级业务对象。背景：CLAUDE.md v2.30「配置中心 v1 上线」
> 中 `soc_data_sources` 类型只覆盖 Wazuh / OpenSearch / Loki / tplink / scanner，AI 模型业务独立表更合理。

#### 5.10.1 表 `soc_ai_providers`【事实，2026-09-13 实测】

| 字段 | 类型 | 约束 |
|---|---|---|
| `id` | BigInteger PK | autoincrement |
| `provider_code` | String(64) | UNIQUE，正则 `^[a-z][a-z0-9-]{2,63}$` |
| `name` | String(200) | NOT NULL，显示名 |
| `base_url` | String(512) | NOT NULL，OpenAI 兼容协议 base URL（结尾不带 `/`） |
| `protocol` | String(32) | NOT NULL default `'openai'`（现仅 openai 兼容协议） |
| `model_name` | String(200) | NOT NULL，如 `glm-4-flash` / `deepseek-chat` |
| `api_key` | Text | NULL，**Fernet 加密存储**；API 出参不返回明文 |
| `scenes` | JSONB | NOT NULL default `'[]'`，如 `["report","asset_query"]` |
| `max_tokens` | Integer | NULL |
| `timeout_seconds` | Integer | NOT NULL default 60 |
| `enabled` | Boolean | NOT NULL default true |
| `is_default` | Boolean | NOT NULL default false（同类型场景只能一个 default） |
| `remark` | String(500) | NULL |
| `last_test_at` / `last_test_ok` / `last_test_message` | 同 `soc_data_sources` | |
| `created_at` / `updated_at` | DateTime(tz) | |

**索引**：`UNIQUE (provider_code)`、`ix_ai_providers_enabled`

#### 5.10.2 场景枚举【事实】

`SCENES` 在 `app/schemas/ai_provider.py` 中定义（9 个）：

| scene value | 中文 | 调用方 |
|---|---|---|
| `asset_query` | 资产 AI 查询 | `services/asset_query.py` |
| `report` | 安全报告 | `services/report_generator.py` |
| `impact` | 变更影响分析 | `services/impact_analysis.py` |
| `ai_analysis` | 告警研判 | `services/ai_analysis.py` |
| `compliance` | 合规 AI 解读 | `services/compliance_ai.py` |
| `knowledge` | 知识库 | `services/knowledge_service.py` |
| `risk` | 风险评分 | `services/asset_risk.py` / `asset_security.py` |
| `reconcile` | 稽核 AI | `services/reconcile_ai.py` |
| `behavior` | 行为画像 | `services/behavior_profile/ai_summary.py` |

#### 5.10.3 场景路由【规格】

`DataSourceResolver.resolve_ai(scene=None)` 路由顺序（详见 §5.7 调用点表）：

1. **`scene` 精确匹配**：`scene` 不为 None 且某启用 provider 的 `scenes` 数组含该 scene → 该 provider
2. **默认实例**：无精确匹配时，`is_default=true` 的启用 provider
3. **任一启用**：仍无则取第一条启用实例（记 `logger.warning`）
4. **env 回落**：DB 无启用 provider 时，`settings.GLM_API_KEY` 兑底（OpenAI 兼容协议）

缓存：进程内 60s，缓存键 `ai:{scene or '*'}`。provider 增删改后调 `invalidate_ai()` 清缓存。

#### 5.10.4 调用约定【规格】

业务代码统一从 `app.services.ai_client` 取 provider：

```python
from app.services.ai_client import get_provider_by_scene, chat

provider = get_provider_by_scene(scene="report")   # 走路由取最佳 provider
text = chat(provider=provider, messages=[...], temperature=0.3)
```

`chat()` 内部统一处理：
- 调用失败降级：返回 `provider is None` 触发各服务自身降级文案（CLAUDE.md §十一 演练通过的 7 个降级路径）
- OpenAI 兼容协议：POST `{base_url}/chat/completions`，Bearer auth 用解密的 `api_key`
- 限时 `provider.timeout_seconds`

#### 5.10.5 迁移 `h3i4j5k6l7m8_ai_providers_independent`【事实】

- 建 `soc_ai_providers` 表 + 索引
- 挂菜单到原 `/system`（2026-09-13 顶级菜单重组后变为 `/config`），菜单名 `aiProvider`、组件 `/system/ai-provider`、权限 `view/add/edit/delete/test`
- admin 授权（soc_role_menus 三列 JSONB 写法）

---

## 6. 前端设计

### 6.1 页面清单与路由【v1.3 与实现对齐】

> **交互原型**：`docs/design/prototype/config-center-prototype.html`（浏览器直接打开，三个页签：数据源列表 / 编辑抽屉 / 配置中心）。下列线框图与原型一致，线框用于快速对照字段，原型用于确认交互与信息架构。

> **【v1.3 重要变更】** 2026-09-13 完成顶级菜单重组（迁移 `i4j5k6l7m8n9`），数据源管理 / 配置中心 / AI 模型管理
> 从「系统管理」移至新建顶级菜单「**配置中心**」(path=`/config`)。下表 URL 路径以**新归属**为准。

| # | 页面 | 路由路径 | 文件 | 备注 |
|---|---|---|---|---|
| 1 | 数据源管理 | `/config/data-source` | `src/views/system/data-source/index.vue` | 原 `/system/data-source` |
| 2 | 配置中心（配置管理） | `/config/config-center` | `src/views/system/config-center/index.vue` | 原 `/system/config-center`；标题 "配置管理" |
| 3 | AI 模型管理 | `/config/ai-provider` | `src/views/system/ai-provider/index.vue` | 原 `/system/ai-provider`（§5.10） |
| ~~4~~ | ~~配置变更审计~~ | ~~`/system/config-logs`~~ | ~~`src/views/system/configLogs/index.vue`~~ | **【v1.3 删除】** `soc_config_change_log` 表与独立页下线，**统一走系统管理 / 审计日志页**（`src/views/system/audit-log/index.vue`，`/system/audit-log/index`） |
| 4 | 审计日志（含配置变更） | `/system/audit-log/index` | `src/views/system/audit-log/index.vue` | admin + auditor 可访问（X1） |

API 文件：`src/api/dataSource.ts`、`src/api/configSchema.ts`、`src/api/aiProvider.ts`、`src/api/audit-log.ts`

路由注册：菜单 URL 由后端 `soc_menus.path` 拼接（约束 C8），ComponentLoader 按 `component` 字段（绝对路径）找文件——所以**菜单归 `/config` 后，前端 .vue 文件位置不变**（仍 `views/system/data-source/`）。`routesAlias.ts` 里 enum 的值是 ComponentLoader 路径，与 URL 解耦，无需同步更新。

### 6.2 页面一：数据源管理（列表页）

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  系统管理  /  数据源管理                                                        │
├──────────────────────────────────────────────────────────────────────────────┤
│  [类型筛选 ▾ 全部]  [🔍 搜索编码/名称        ]   [批量测试]  [+ 新增数据源]      │
├──────────────────────────────────────────────────────────────────────────────┤
│  编码          类型        名称           地址              状态   健康   操作  │
│  ───────────────────────────────────────────────────────────────────────────  │
│  wazuh-prod    Wazuh    生产 Wazuh      https://192.168.0.40:55000            │
│                                                          [默认][启用] ● 正常   │
│                                                          测试 编辑 更多▾       │
│  ───────────────────────────────────────────────────────────────────────────  │
│  loki-main     Loki     主 Loki        http://192.168.0.30:3100               │
│                                                          [默认][启用] ● 正常   │
│                                                          测试 编辑 更多▾       │
│  ───────────────────────────────────────────────────────────────────────────  │
│  wazuh-branch  Wazuh    分支 Wazuh      https://10.0.1.9:55000                │
│                                                          [     ][停用] ○ 未测  │
│                                                          测试 编辑 更多▾       │
├──────────────────────────────────────────────────────────────────────────────┤
│  当前生效来源：Wazuh → 数据库 wazuh-prod ｜ Loki → 数据库 loki-main             │
│                OpenSearch → 环境变量（未配置数据源实例）                         │
└──────────────────────────────────────────────────────────────────────────────┘
```

**"更多▾" 菜单项**：设为默认 / 启用 / 停用 / 删除

**健康状态列语义**

| 显示 | 条件 |
|---|---|
| ● 正常（绿） | `last_test_ok = true` 且 24h 内有测试记录 |
| ● 异常（红） | `last_test_ok = false` |
| ○ 未测（灰） | `last_test_at IS NULL` |
| ◐ 过期（黄） | `last_test_ok = true` 但超过 24h |

### 6.3 页面二：新增/编辑数据源（右侧抽屉，宽 720px）

```
┌────────────────────────────────────────────────────────────┐
│  新增数据源                                            [×]  │
├────────────────────────────────────────────────────────────┤
│  基本信息                                                    │
│  ┌────────────────────┬──────────────────────────────────┐ │
│  │ 数据源编码 *       │ 数据源类型 *                       │ │
│  │ [wazuh-prod      ] │ [Wazuh              ▾]           │ │
│  │ 小写字母数字连字符 │                                    │ │
│  └────────────────────┴──────────────────────────────────┘ │
│  显示名称 *                                                  │
│  [生产 Wazuh（192.168.0.40）                              ] │
│                                                              │
│  连接参数                                                    │
│  服务地址 *                                                  │
│  [https://192.168.0.40:55000                             ] │
│  认证方式 [Basic 用户名密码 ▾]  超时(秒) [30   ]             │
│  用户名   [wazuh                                          ] │
│  密码/密钥 [••••••••••••]  ← 编辑态显示占位；留空=不修改      │
│  ☐ 校验 SSL 证书   重试次数 [3]   退避(秒) [2]              │
│                                                              │
│  采集范围（Wazuh）                                           │
│  ☑ 资产 ☑ 漏洞 ☐ 基线        同步间隔(秒) [300]             │
│                                                              │
│  ☐ 启用此数据源   ☐ 设为同类型默认实例                        │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 测试结果                                               │  │
│  │ ✅ 认证成功  ✅ 版本 4.7.2  ✅ Agents 可达             │  │
│  │ ⚠️  /vulnerability 404 — 本环境不支持，CVE 走 OpenSearch│  │
│  │ 耗时 128ms                                             │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│                     [测试连接]   [取消]  [保存]              │
└────────────────────────────────────────────────────────────┘
```

**字段校验规则【规格】**

| 字段 | 规则 | 错误提示 |
|---|---|---|
| `source_code` | 必填；`^[a-z][a-z0-9-]{2,63}$`；新建时远端校验唯一 | "编码只能包含小写字母、数字和连字符，3-64 位" |
| `source_type` | 必填，枚举 | |
| `name` | 必填，≤200 字符 | |
| `endpoint` | 必填；必须以 `http://` 或 `https://` 开头；不以 `/` 结尾 | "地址需以 http:// 或 https:// 开头" |
| `timeout_seconds` | 1–300 | |
| `retry_times` | 0–10 | |
| `retry_backoff_seconds` | 1–60 | |
| `auth_secret` | 新建且 `auth_type != none` 时必填；编辑时留空表示不修改 | |

**交互细节【规格】**

| 编号 | 规则 |
|---|---|
| I1 | 测试连接失败时允许保存，但弹二次确认："测试未通过，确定仍要保存？保存后该数据源将被标记为异常。" |
| I2 | 保存成功后 toast 提示生效方式："已保存，🟢 立即生效" |
| I3 | 密码框 `autocomplete="new-password"`，编辑态回显固定 `******`，聚焦清空；未修改则提交时传 `null` |
| I4 | 设为默认时若同类型已有默认，提示："将把 `xxx` 取消默认，继续？" |
| I5 | 删除唯一启用实例时后端返回 409，前端展示后端 message |
| I6 | 所有写操作需 admin；非 admin 时按钮置灰并 tooltip 提示 |

### 6.4 页面三：配置中心（Schema 驱动表单）

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  系统管理 / 配置中心                                                           │
├──────────────┬───────────────────────────────────────────────────────────────┤
│ 配置分类      │  告警治理 (alert_governance)                    [恢复默认]      │
│              │  ─────────────────────────────────────────────────────────    │
│ ▸ 告警治理 (3)│  告警研判                                                      │
│ ▸ 行为检测(18)│  每日 AI 研判 TopN 簇数 *            [ 20 ]  🟢 立即生效        │
│ ▸ 风险规则 (5)│  每日参与 AI 研判的告警簇数量，成本约 N 次/天调用                │
│ ▸ 推送规则 (4)│                                                               │
│ ▸ 报告     (6)│  噪声抑制                                                      │
│              │  告警簇最小条数 *                     [ 1  ]  🟡 下周期生效      │
│              │  少于该值的簇不进入摘要/研判                                     │
│              │                                                               │
│              │  噪声抑制规则 ID                [ 留空则不过滤    ]  🟢          │
│              │  逗号分隔                                                      │
│              │                                                               │
│              │                        [取消]  [保存]                          │
└──────────────┴───────────────────────────────────────────────────────────────┘
```

**渲染规则【规格】**

| value_type | 渲染控件 |
|---|---|
| `string` | `ElInput` |
| `number` | `ElInputNumber`（取 `validation.min/max`） |
| `boolean` | `ElSwitch` |
| `json` | `ElInput type=textarea` + JSON 格式校验（失焦时） |
| `password` | `ElInput type=password`，`sensitive=true`，出参脱敏 |
| `multiline` | `ElInput type=textarea :rows=4` |
| `list` | `ElSelect multiple`（取 `options`） |

- 无 schema 记录的配置项**仍以通用 KV 编辑器兜底显示**，避免因缺 schema 导致配置项不可见
- 每个字段下方显示 `help_text`，右侧显示 `effect_scope` 徽标
- `sensitive=true` 的字段：编辑框留空表示不修改；展示为 `******`
- `editable=false` 的字段置灰

### 6.5 菜单注册 SQL【规格，v1.3 重写】

> **【v1.3 变更】**
> - 移除 `configLogs` 菜单（配置审计已合并到「审计日志」页）
> - 新增 `aiProvider` 菜单（§5.10）
> - 父菜单从 `/system` 改 `/config`（2026-09-13 顶级菜单重组迁移 `i4j5k6l7m8n9`）
> - `configCenter` 标题从"配置中心"改为"配置管理"（下级菜单以"配置中心"为顶级、"配置管理"为下级）
> - 同一迁移 `d2e3f4g5h6i7` / `h3i4j5k6l7m8` / `e2f3g4h5i6j7` 已落地，生产 head 已同步

**菜单约定**（与 v1.0 一致，重申）：

| 字段 | 顶级菜单 | 子菜单 | 反例（v1.0 写法） |
|---|---|---|---|
| `path` | 带前导斜杠 `/config` | **相对名** `data-source`、`config-center` | ❌ `/config/data-source`（会拼成 `/config/config/data-source`） |
| `component` | `/index/index`（Layout） | **带前导斜杠的实际路径** `/system/data-source`、`/system/config-center` | ❌ `system/dataSource/index.vue`（ComponentLoader 解析不到，404） |
| `icon` | iconify `ri:*` | 同左 | ❌ `Database` / `Setting` / `Document`（Material 风格，**静默不渲染**） |
| `permissions` | 容器为 `[]` | **`[{title,authMark}]` 必填** | ❌ 缺失 → 前端 `hasAuth('add')` 恒 false，按钮全不显示 |

```sql
-- 1) 顶级菜单：配置中心 /config (parent_id=NULL)，sort=9
INSERT INTO soc_menus (parent_id, name, title, path, icon, component, sort_order, is_visible, permissions)
SELECT NULL, '配置中心', '配置中心', '/config', 'ri:settings-2-line',
       '/index/index', 9, true, CAST('[]' AS jsonb)
WHERE NOT EXISTS (
  SELECT 1 FROM soc_menus WHERE path = '/config' AND parent_id IS NULL
);

-- 2) 子菜单：挂 /config 下，子查询取 parent_id（不硬编码）
INSERT INTO soc_menus (parent_id, name, title, path, icon, component, sort_order, is_visible, permissions)
SELECT p.id, v.name, v.title, v.path, v.icon, v.component, v.sort_order, true, CAST(v.perms AS jsonb)
FROM (SELECT id FROM soc_menus WHERE path = '/config' AND parent_id IS NULL) p
CROSS JOIN (VALUES
  ('configCenter', '配置管理', 'config-center', 'ri:equalizer-line',
   '/system/config-center', 1,
   '[{"title":"查看","authMark":"view"},{"title":"编辑","authMark":"edit"}]'),
  ('dataSource',   '数据源管理', 'data-source',  'ri:database-2-line',
   '/system/data-source',   2,
   '[{"title":"查看","authMark":"view"},{"title":"新增","authMark":"add"},{"title":"编辑","authMark":"edit"},{"title":"删除","authMark":"delete"},{"title":"连接测试","authMark":"test"}]'),
  ('aiProvider',   'AI 模型管理', 'ai-provider',  'ri:robot-2-line',
   '/system/ai-provider',   3,
   '[{"title":"查看","authMark":"view"},{"title":"新增","authMark":"add"},{"title":"编辑","authMark":"edit"},{"title":"删除","authMark":"delete"},{"title":"连接测试","authMark":"test"}]')
) AS v(name, title, path, icon, component, sort_order, perms)
WHERE NOT EXISTS (
  SELECT 1 FROM soc_menus m WHERE m.name = v.name AND m.parent_id = p.id
);

-- 3) admin 授权（role_id 用子查询按 code 取；soc_role_menus 只有三列）
INSERT INTO soc_role_menus (role_id, menu_id, permissions)
SELECT r.id, m.id,
       CAST((SELECT jsonb_agg(e->>'authMark') FROM jsonb_array_elements(m.permissions) e) AS jsonb)
FROM soc_roles r
CROSS JOIN soc_menus m
WHERE r.code = 'admin'
  AND m.name IN ('configCenter', 'dataSource', 'aiProvider')
  AND NOT EXISTS (
    SELECT 1 FROM soc_role_menus rm WHERE rm.role_id = r.id AND rm.menu_id = m.id
  );
```

**执行后必须回读确认**（`INSERT ... SELECT` 在父菜单/角色解析不到时会**静默插 0 行且不报错**）：

```sql
SELECT m.parent_id, p.path AS parent_path, m.name, m.title, m.path, m.component, m.icon,
       (SELECT count(*) FROM soc_role_menus rm WHERE rm.menu_id = m.id) AS granted_roles
FROM soc_menus m
LEFT JOIN soc_menus p ON m.parent_id = p.id
WHERE m.name IN ('configCenter','dataSource','aiProvider')
ORDER BY m.sort_order;
-- 期望 3 行，parent_path='/config'，granted_roles 均 >= 1
```

> **其他角色授权**：本期三个页面均为 admin 专属（写端点挂 `require_admin()`）。
> `auditor` 不需要这三个菜单（仅「审计日志」页可查），X1 已修：菜单树按子菜单粒度过滤，授父容器不会连带放行子菜单。

> 菜单为后端驱动；新增后前端需刷新或重新登录才能看到（约束 C8）。

### 6.6 API 文件范式【规格】

`src/api/dataSource.ts`：

```ts
import request from '@/utils/http'

const BASE = '/api/v1/data-sources'

export function getDataSourceList(params?: any) {
  return request.get({ url: BASE, params, keepFullResponse: true })
}
export function getDataSourceTypes() {
  return request.get({ url: `${BASE}/types` })
}
export function addDataSource(data: any) {
  return request.post({ url: BASE, data, showSuccessMessage: true, successMessage: '新增成功' })
}
export function updateDataSource(id: number, data: any) {
  return request.put({ url: `${BASE}/${id}`, data, showSuccessMessage: true, successMessage: '更新成功' })
}
export function deleteDataSource(id: number) {
  return request.del({ url: `${BASE}/${id}`, showSuccessMessage: true, successMessage: '删除成功' })
}
export function testDataSource(payload: { id?: number; draft?: any }) {
  return request.post({ url: `${BASE}/test`, data: payload })
}
export function setDefaultDataSource(id: number) {
  return request.post({ url: `${BASE}/${id}/set-default`, showSuccessMessage: true })
}
export function toggleDataSource(id: number, enabled: boolean) {
  return request.patch({ url: `${BASE}/${id}/enabled`, data: { enabled }, showSuccessMessage: true })
}
export function getResolveStatus() {
  return request.get({ url: `${BASE}/resolve-status` })
}
```

---

## 7. 约束与非功能要求

### 7.1 安全约束【规格】

| # | 约束 | 实现方式 |
|---|---|---|
| S1 | **密钥只写不读** | 出参 Schema 不含 `auth_secret`；列表/详情/审计一律脱敏 |
| S2 | 密钥加密存储 | `encryption_service.encrypt()` 写入，读取时 `decrypt_if_needed()` |
| S3 | 写操作 admin-only | 端点 `dependencies=[Depends(require_admin())]` |
| S4 | 审计不落明文 | `ConfigAuditService` 对敏感键替换为 `***` |
| S5 | 不记录完整 URL 中的凭据 | 日志中 endpoint 只记 host，不记 userinfo |
| S6 | 测试连接不产生副作用 | Wazuh 仅只读 GET；tplink 只做 TCP 探测，不做真实登录 |
| S7 | 超时硬上限 | `min(timeout_seconds, 15)`，防止界面卡死 |

### 7.2 兼容性约束【规格】

| # | 约束 |
|---|---|
| B1 | DB 无配置时必须回落 `settings`，**现有部署行为不得改变** |
| B2 | resolver 异常一律吞掉并回落，不得中断业务 |
| B3 | `config_json` 未知键保留，不做强校验 |
| B4 | 新增字段全部可空或有默认值，不影响既有行 |

### 7.3 性能约束【规格】

| # | 约束 |
|---|---|
| P1 | resolver 缓存 60s，与既有配置模块一致 |
| P2 | 测试连接并发上限：单次请求一个数据源，批量测试串行执行，前端禁用重复点击 |
| P3 | 列表查询走索引 `(source_type, enabled)` |

### 7.4 合规

- 配置中可能包含个人/组织信息（如通知邮箱、Webhook 地址），审计日志**仅记录变更字段与脱敏值**，不记录完整值
- 界面提示：涉及密码的配置项仅管理员可见可改

### 7.5 可观测性

| 项 | 要求 |
|---|---|
| 日志 | resolver 回落时 `logger.warning("data source %s fallback to env: %s", type, reason)` |
| 指标 | 可选：暴露 `/metrics` 中 `config_resolve_total{type,origin}` 计数器（v2） |

---

## 8. 验收标准（Acceptance Criteria）

> 每条必须可独立验证。AC-01 ~ AC-08 对应 §3.2 用户故事。

| ID | 验收标准 | 验证方式 |
|---|---|---|
| AC-01 | 界面可新增 Wazuh 数据源，填写地址/账号/密码后保存成功；**无需重启后端**，60s 内 `WazuhClient` 使用新地址 | 改库中 endpoint 为错误地址 → 60s 后触发同步 → 日志出现连接错误（证明已读 DB） |
| AC-02 | 点击"测试连接"返回结构化结果：认证/版本/Agents/漏洞接口四项检查 + 耗时 | 对正常 Wazuh 测试，返回 `ok=true` 且 `checks` 含 4 项 |
| AC-03 | 可创建两个 `source_type=wazuh` 实例，设置其中一个为默认后，另一个自动取消默认 | DB 查询 `is_default=true` 同类型仅 1 行 |
| AC-04 | 每个配置项显示生效方式徽标；改 `browsing_detection.interval_seconds` 提示"下周期生效" | 页面可见徽标 |
| AC-05 | 保存密码后，再次打开编辑框显示 `******`；列表/详情接口响应中**不含** `auth_secret` 字段 | 抓包验证 |
| AC-06【v1.3】| 修改数据源后，`soc_audit_logs` 新增一条记录（`resource_type=data_source`），含 `username`、`old_values`、`new_values`，敏感字段值为 `***`，并参与 hash 链 | DB 查询；同 commit 547346e 修复 operator 字段（原记为 `system`） |
| AC-07 | 列表页健康列根据 `last_test_ok` 显示绿/红/灰/黄四种状态 | 构造数据验证 |
| AC-08 | 删除同类型唯一启用实例被拒，返回 409 且前端提示后端 message | 调用 API 验证 |
| AC-09 | DB 中无 wazuh 数据源时，系统行为与改造前完全一致（回落 `.env`） | 清空 `soc_data_sources` → 同步功能正常 |
| AC-10 | `pytest tests/security/test_route_authorization.py` 通过 | CI |
| AC-11 | 非 admin 用户登录，写按钮置灰，直接调 API 返回 403 | 接口测试 |
| AC-12 | 前端类型检查通过：`cd src/frontend && ./node_modules/.bin/vue-tsc --noEmit` | CI（仅允许既有 login 页报错） |
| AC-13【v1.3】| AI Provider 配置生效：新增/启用一个 ai provider，60s 内 `ai_client.get_provider_by_scene(scene)` 返回该 provider（精确场景路由优先于默认实例优先于 env 兑底） | 修改 `soc_ai_providers` 验证缓存 invalidate 行为 |
| AC-14【v1.3】| 配置审计去重：`grep -r soc_config_change_log src/` 仅出现在文档历史与 migration 文件，**业务代码零引用**；`/audit-logs?resource_type=data_source` 能查到变更记录 | DB 查询 + 代码 grep |

---

## 9. 实施步骤（WBS）

| 步骤 | 任务 | 产出 | 依赖 |
|---|---|---|---|
| W1 | 建 3 张模型文件 + 注册 `models/__init__.py` | `data_source.py`、`config_schema.py`、`config_change_log.py` | — |
| W2 | 执行 `ALTER TABLE soc_source_health ADD COLUMN IF NOT EXISTS source_id BIGINT` | SQL 脚本 + 执行记录 | — |
| W3 | 启动后端验证 3 表已建 | 启动日志 / `\d soc_data_sources` | W1 |
| W4 | Schema 文件（`schemas/data_source.py`、`config_schema.py`） | | W1 |
| W5 | `ConfigAuditService` | | W1 |
| W6 | `DataSourceService`（CRUD + 设默认 + 删除保护 + 审计） | | W4/W5 |
| W7 | `DataSourceResolver`（缓存 + 回落） | | W4 |
| W8 | `ConfigTestService`（按类型探测） | | — |
| W9 | `api/data_sources.py` + 路由注册 | | W6/W7/W8 |
| W10 | 跑 `pytest tests/security/test_route_authorization.py` | 通过 | W9 |
| W11 | 接入 `wazuh_client.py` / `loki_client.py` / `opensearch_scap_sync.py` | | W7 |
| W12 | `scripts/seed_config_schema.py` 并执行 | 种子数据 | W1 |
| W13 | 前端 API 文件 + 3 个页面 + 路由 | | W9 |
| W14 | 菜单 SQL 执行 + 关联 admin | | W13 |
| W15 | 端到端验收（§8 全项） | 验收报告 | 全部 |

**建议提交粒度**：W1–W3 一个 commit（模型 + Alembic 迁移）、W4–W10 一个 commit（后端）、W11 一个 commit（接入）、W12–W14 一个 commit（前端）。

> 迁移文件**写完先 review 再跑**——本地已 upgrade 后再改文件，`alembic_version` 已记录新版本，重跑不再执行，只能 downgrade→upgrade 或手改 DB（CLAUDE.md 已踩过）。

---

## 10. 风险与回滚

| 风险 | 影响 | 对策 | 回滚方式 |
|---|---|---|---|
| resolver 改动破坏现有同步 | 数据断流 | 严格 B1/B2 回落；W11 单独 commit | `git revert` W11 commit |
| 部分唯一索引在旧 PG 版本不支持 | 建表失败 | 降级为 Service 层事务实现 | 删索引，改代码 |
| 测试连接触发 Wazuh 限流 | 数据源被临时封禁 | 只做只读 GET；批量测试串行 | 停用批量测试 |
| 加密密钥变更导致 `auth_secret` 无法解密 | 数据源全部失效 | 解密失败时记 error 并提示"密钥已变更，请重新录入密码"，**不要**静默回落 | 重新录入 |
| 菜单插错 parent_id / 静默插 0 行 | 菜单不显示或位置异常 | 子查询取 parent_id，**执行后回读确认 3 行**（§6.5） | 迁移 downgrade |
| 菜单 component/icon/permissions 格式错 | 页面 404 / 无图标 / 按钮全隐藏（均无报错） | 严格按 §6.5 表格约定；上线后人工点一遍 UI | 修数据 |

---

## 11. 附录

### 11.1 关键现有文件索引

| 文件 | 用途 |
|---|---|
| `app/core/config.py` | `Settings`，L1 环境变量入口；【v1.3】`GLM_API_KEY` 已改 Optional |
| `app/core/permissions.py:29` | `require_admin()` |
| `app/core/route_security.py` | `enforce_write_default`、授权标记 |
| `app/core/route_allowlist.py` | public/machine 路由白名单 |
| `app/core/audit_decorator.py` | `@log_audit` 装饰器（AI Provider / 数据源写端点均用） |
| `app/api/__init__.py` | 路由注册（`include_human_router`），data-sources / config-schemas / ai-providers / system-configs / audit-logs 均在此挂 |
| `app/services/encryption_service.py` | Fernet 加解密 |
| `app/services/browsing_detection/config.py` | **L2 配置读取最佳实践样板**（60s 缓存 + 默认值） |
| `app/services/alert_governance_config.py` | L2 样板之二（默认值自注册） |
| `app/models/source_health.py` | `soc_source_health`（【v1.3】含 `source_id` 列） |
| `app/models/data_source.py` | `soc_data_sources`（Wazuh / OpenSearch / Loki / tplink / scanner） |
| `app/models/config_schema.py` | `soc_config_schema`（配置 Schema 注册表） |
| `app/models/ai_provider.py` | `soc_ai_providers`（【v1.3】独立于数据源） |
| `app/services/data_source_resolver.py` | `DataSourceResolver` + `get_endpoint`/`get_config` 便捷函数 + `resolve_ai` 场景路由 |
| `app/services/ai_client.py` | `get_provider_by_scene()` + `chat()` 业务调用入口（【v1.3】） |
| `app/services/ai_provider_service.py` | AI Provider 增删改查 + invalidate_ai()（【v1.3】） |
| `app/services/data_source_service.py` | 数据源 CRUD + 审计 |
| `app/services/config_test_service.py` | 三大数据源 + AI Provider 测试连接 |
| `app/services/config_audit_service.py` | **【v1.3】**内部转调 `AuditLogService.create_audit_log`，写 `soc_audit_logs` |
| `app/services/config_validation.py` | Schema 驱动的值校验 + 删除保护（系统配置端点复用） |
| `app/services/audit_log_service.py` | `AuditLogService.create_audit_log()` —— ConfigAuditService 底层调用 |
| `app/services/wazuh_client.py` | Wazuh 客户端（接入点） |
| `app/api/data_sources.py` | `/api/v1/data-sources` 路由 |
| `app/api/config_schemas.py` | `/api/v1/config-schemas` 路由（只读） |
| `app/api/ai_providers.py` | `/api/v1/ai-providers` 路由（【v1.3】） |
| `app/api/system_configs.py` | `/api/v1/system-configs` 既有路由（高级配置页复用） |
| `app/api/audit_logs.py` | `/api/v1/audit-logs` 路由（【v1.3】接管配置审计） |
| `scripts/seed_config_schema.py` | schema 种子数据 |
| `scripts/seed_datasources.py` | Wazuh/OpenSearch/Loki 三个默认实例（生产手跑） |
| `scripts/seed_ai_provider.py` | AI Provider 初始 seed（【v1.3】） |
| `.env.example` | 2026-09-11 新建的配置模板；【v1.3】`GLM_API_KEY` 已标记可选 |

### 11.2 `source_type` 枚举（前端与后端共用）

```json
[
  {"value": "wazuh",      "label": "Wazuh",      "auth_types": ["basic"],              "defaultPort": 55000},
  {"value": "opensearch", "label": "OpenSearch", "auth_types": ["basic", "none"],      "defaultPort": 9200},
  {"value": "loki",       "label": "Loki",       "auth_types": ["none", "token"],      "defaultPort": 3100},
  {"value": "tplink",     "label": "TP-Link 路由器", "auth_types": ["basic"],          "defaultPort": 80},
  {"value": "scanner",    "label": "扫描器",      "auth_types": ["apikey", "none"],     "defaultPort": 8000}
]
```

### 11.3 常见错误（**执行前必读**）

| 错误 | 后果 | 正确做法 |
|---|---|---|
| 直接用 `api_router.include_router` | CI 路由授权检查失败 | 用 `include_human_router` |
| 写端点忘加 `require_admin()` | CI 失败 / 越权 | 显式加 dependencies |
| 新模型忘记在 `models/__init__.py` 导入 | 表不创建 | 必加 |
| 靠 `create_all` 建表、跳过 Alembic | 加列不生效、无法回退、新环境重建必漏 | 走 Alembic 迁移（D5 v1.1）|
| 菜单 `component` 写成 `xxx/index.vue` | ComponentLoader 解析不到，页面 404 | 写 `/system/data-source` |
| 菜单 `icon` 用 `Database`/`Setting` | iconify 静默失败，无图标无报错 | 用真实存在的 `ri:*` |
| 子菜单 `permissions` 留空 | 前端 `hasAuth()` 恒 false，按钮全隐藏 | 子菜单自身必填 JSONB |
| 菜单 SQL 硬编码 `parent_id` / `role_id` | 插错位置或静默插 0 行 | 一律子查询 + 事后回读确认 |
| 出参 Schema 声明了 `auth_secret` | 密钥泄露 | 不声明该字段 |
| 把 `soc_asset_sources` 当数据源表用 | 语义冲突 | 用 `soc_data_sources` |
| resolver 未 try/except | 配置异常导致业务中断 | 一律回落 settings |
| 【v1.3】`include_human_router` prefix 与 endpoint path 错位（例 `/data-source` 单数 vs `/data-sources` 复数） | 前端 `BASE` 对不上、返回 404 | prefix 必须与 endpoint 完整路径一致 |
| 【v1.3】AI Provider 接表读 DB 代替读 `soc_data_sources` | 场景路由失效 | `DataSourceResolver.resolve_ai()` 走独立 `soc_ai_providers`（§5.10.3） |
| 【v1.3】配置审计另起独立表 `soc_config_change_log` | 绕开 hash 链防篡改 | 写 `soc_audit_logs`（`AuditLogService.create_audit_log`） |
| 【v1.3】推测「多 head」却不实测 `alembic heads` | 错误地引入 merge 步骤、迁移依赖被误判 | 写迁移前先跑 `alembic heads` 现场确认 |

