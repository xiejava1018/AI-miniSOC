# 资产详情子域关联整改 — 验收报告

> 验收日期：2026-08-16
> 验收对象：commit `22a4828`（T0a+M0-M2）+ commit `be6e2c9`（M3-M5+T0b），工作树干净
> 方法：代码静态核对 + 真实环境（远程 Postgres `111.228.57.2:25432` / OpenSearch `192.168.0.40:9200` 均可达）接口冒烟 + 前端 `vue-tsc` 类型检查
> 验收角色：设计 Agent 最终检查（pi agent 实施，本 Agent 对账）

---

## 一、验收结论

✅ **验收通过。** pi agent 的实现与整改方案一致，且**超出约定最小批次**完整交付了全计划（M3 应用清单 / M4 端口双源 / M5 告警增强 / T0b 资产合并）。所有功能端到端验证可用，**无阻断性缺陷**。上一轮检查报告标记的「提交不自洽 / 大量未提交」问题已在 `be6e2c9` 中修复。

---

## 二、全功能验证矩阵（实测证据）

| 工单 | 验证方式 | 结果 | 证据 |
|---|---|---|---|
| **T0a** 关联键去重+唯一索引 | 直连真实库 `pg_indexes` + 双挂核查 | ✅ | `uq_soc_assets_agent_id` 索引**存在**；`wazuh_agent_id` 双挂 **0 行**；全表 74 资产 / 22 带 agent |
| **T1** 摘要去伪 | `GET /assets/{id}/summary` | ✅ | 真实统计 `vuln_critical/high/total/sca_failed`，随资产变化，非硬编码；`applications` 现取真实值（M3 已贯通） |
| **T2** SCA 卡判显 | 代码核对 vue L111-119 | ✅ | `sca_pass_rate=null` 时显"SCA 不合规"+`sca_failed`+副标"合规率待接入(P5)" |
| **T3** 漏洞 Tab | 代码核对 vue L191-256 | ✅ | 真实表格 + AI 风险列 + "生成事件"按钮(`createIncidentFromVulnerability`) + 无 Agent 空态 |
| **T4** risk_score + vuln_type | `asset-vulnerabilities?asset_id&vuln_type=scap` | ✅ | `CVE-2024-6789` cvss=5.0→risk_score=24.5；`CVE-2024-2345` cvss=7.5→34.5（计算正确）；`vuln_type=sca` 正确分离基线 |
| **M3** 应用清单 | `GET /assets/{id}/applications` | ✅ | `code=200, total=546/704`，返回真实包（Cheetah/Flask/GeoIP…）；OpenSearch `wazuh-states-inventory-packages-*` 直查 |
| **M4** 端口双源 | `GET /assets/{id}/wazuh-ports` | ✅ | agent 009 返回 **15 条**真实监听端口（22/ssh、80/nginx、111/rpcbind、443…）；本地 CRUD + Wazuh 双源合并；"关联漏洞"列 `matchedVulns` 按服务名匹配 |
| **M5** 告警增强 | `GET /alerts/?agent_id=&hours=&level=` | ✅ | 系统总量 **376,278** 条；端点正确支持时间范围/等级/分页/agent 过滤；前端 Tab 三筛选齐全 |
| **T11** 类型对齐 | `AssetSummary` 类型 + vue | ✅ | 类型含 `sca_failed`，模板正确引用 |
| 后端启动 | uvicorn + `/health` | ✅ | 200，无导入错误（含 `wazuh_inventory_service.py` 依赖已满足） |
| 前端类型 | `vue-tsc --noEmit` | ✅ | `asset/detail`、`api/asset`、`api/vulnerabilities` **零类型错误**（仅 `login/index.vue` 既有报错，无关） |

---

## 三、与方案的偏差 / 需知悉事项

1. **范围超出（净增益，非缺陷）**：约定批次为 T0a+M0-M2，pi agent 实际一次性交付了**全计划**（M3/M4/M5/T0b）。commit message 写"M3-M5+T0b"功能上属实（M3/M4 逻辑内联于 `wazuh_inventory_service.py`+`assets.py`+vue，未单列 service 文件，属正常封装）。
2. **M4 端口↔漏洞关联为启发式**：`matchedVulns`（vue L1050）用"端口进程/服务名子串匹配漏洞 title/cve_id"做关联，非严格 CVE↔端口映射。属 M4 设计的 MVP 级实现，后续可增强（如基于 CPE/服务指纹精确匹配）。
3. **M5 个别资产空态属数据现象**：agent 009 近 90 天确为 0 告警（系统总量 37.6 万、其它 agent 数千至 16 万条），前端空态正确，非缺陷。
4. **T0b 合并脚本**：`merge_duplicate_assets_t0b.py` 已提交（103 行），为一次性运维脚本；T0a 索引已建、双挂已 0，说明去重目标已达成（脚本可保留备用，非必须再跑）。

---

## 四、上一轮「重大缺陷」处置状态

> 上一轮（2026-08-15 检查报告）标记的两条问题，**本次验收确认均已消除**：

- ~~`22a4828` 引用未提交 `wazuh_inventory_service.py` 致干净检出崩溃~~ → `be6e2c9` 已提交该文件，HEAD 自洽。
- ~~大量改动未提交~~ → 当前 `git status` 干净，全部落库。

---

## 五、验收方自查说明（透明度）

验收过程中，我方冒烟脚本一度因响应信封路径取错（`d['items']` 应为 `d['data']['items']`），**误报 M4 端口为 0**。经受控复测（直连端点原始响应）确认 M4 实际正常返回 15 条端口，前述"0"为**验收脚本解析错误，非代码缺陷**。其余结论（T0a/T1/T4/M3/M5）均经多轮独立核实，结论稳固。

---

## 六、一句话总结

> 方案落地完整、功能可用、提交自洽；M4 端口与 M5 告警均经真实数据验证。可放心合并/上线。唯一可后续增强项：M4 端口↔漏洞关联精度。
