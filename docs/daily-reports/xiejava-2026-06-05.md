# 开发日报

**日期**：2026-06-05
**开发者**：xiejava
**项目**：AI-miniSOC（AI驱动的微型安全运营中心）

## 📍 当前阶段

Phase 1: 基础完善（进行中）。今日把 Art Bot 聊天的 LLM 后端从智谱 GLM 切到本地 Claude Code CLI 子进程，为 Phase 2（AI 能力）打通调用通路。

## ✅ 今日完成

### 摘要

把 Art Bot 切到 Claude Code CLI 子进程，新增 19 个 mock subprocess 单测，3ae00bc 已推远端。

### 详细列表

- `3ae00bc` ✨ feat(ai): chat_service 改用 Claude Code CLI 子进程
  - `src/backend/app/services/chat_service.py`（新写，354 行）— `asyncio.create_subprocess_exec` 调本地 `claude`，`--session-id` 复用 DB 会话，`--no-session-persistence` 防止本地 session 污染
  - `src/backend/app/core/config.py`（+10 行）— 8 个 `CLAUDE_CLI_*` 配置项（路径/模型/超时/工具白名单/workspace/effort/dangerously-skip/no-persist）
  - `src/backend/tests/services/test_chat_service.py`（新写，573 行）— 19 个单测，mock subprocess 覆盖 CRUD、CLI 命令拼装、stream-json 流式解析、客户端断开 cancel → `proc.terminate()`/`kill()` 路径
  - 踩坑记录（已写进 commit message）：
    1. `--output-format=stream-json` 必须配 `--verbose`，否则 CLI 报错 exit 1
    2. `--include-partial-messages` 会同时推 `thinking_delta` 和完整 assistant 消息，前端拼出双倍文本 → 去掉
    3. pytest 已在 event loop 内，测试里跑协程不能 `asyncio.run()`，改用 `asyncio.new_event_loop().run_until_complete()`
- 前端 `src/frontend/.env.development` 修 `VITE_API_PROXY_URL 8080→8000`（顺手修，原本连不上后端导致登录 500）
- 前后端重启验证：SSE 流式回话正常，单 delta 无重复，浏览器可见完整 Markdown（表格/代码块保留）

## 🔬 测试结果

```
$ ../../venv/bin/python -m pytest tests/services/test_chat_service.py -v
================= 19 passed, 721 warnings in 165.17s (0:02:45) =================
```

- 19/19 PASSED
- 全部 mock subprocess，无真实 CLI 调用

## 🔄 Git 工作流

- 今日 1 commit `3ae00bc`，已 `git push origin master`
- 工作树仍有其它 untracked / modified（其它会话的改动，未提交，与本日报无关）

## 📊 进度

- **本次完成率**：100%（Art Bot CLI 改造闭环：代码 + 配置 + 测试 + 文档 + 重启验证）
- **整体 Phase 1 完成度**：88% → **89%**
- **测试基线**：56 → **63 in-process 测试 pass**（19 新增 + 44 沿用自 5/29 基线）
- **未计入 pre-existing 失败**：`tests/integration/test_user_workflow.py::test_user_lifecycle` 仍 fail（envelope 设计 vs 断言风格不匹配）

## 🎯 明日计划

1. **修 `ENCRYPTION_KEY`** — pre-existing 启动 warning，重启丢加密数据；按 `docs/development/security-checklist.md` 生成合法 Fernet 密钥
2. **修 envelope 断言** — `tests/integration/test_user_workflow.py::test_user_lifecycle` 改成 `body["code"] == 201`，与 envelope 设计对齐
3. **前端把 chat 视图接上 `useChatStore`** — 今日发现 `src/frontend/src/store/modules/chat.ts` 定义了 `useChatStore`，但 `views/` 与 `router/` 都没引用，浏览器侧 Art Bot 入口实际未跑通
4. **跑全量回归** — `pytest tests/ -v`，确认 CLI 改造没误伤其它模块

## ⚠️ 阻塞问题

| 问题 | 影响 | 对策 |
|------|------|------|
| _（无）_ | — | — |

## 📝 技术决策

| 决策 | 原因 | 影响 |
|------|------|------|
| Art Bot 改用 Claude Code CLI 子进程（而非 Anthropic Python SDK） | CLI 自管 OAuth/Keychain 鉴权，运维侧无需管理 `ANTHROPIC_API_KEY`；避免在前端/后端多处落地 Key | 失去 Python 端对 stream 的细粒度控制（无 `text_stream` helper、需自己解析 `stream-json`）；首次启动可能需 `claude auth login`；CLI 升级可能改 schema，需关注 `claude --version` 兼容性 |

---

*生成时间：2026-06-05 17:55 | Claude Opus 4.8 辅助生成*
